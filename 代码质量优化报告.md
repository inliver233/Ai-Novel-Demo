# 代码质量优化报告（ainovel Atelier）

审查日期：2025-12-26  
审查角色：以“大厂（Google）Mentor”标准进行全栈代码 Review（可维护性/可靠性/一致性/安全/性能/可观测性/UI/隐蔽 bug）  
输出约束：本次不改任何业务代码；仅输出问题与“可拆分可验收”的整改任务清单（开发按任务逐项完成即可完成重构闭环）。

---

## 0. 结论（TL;DR）

项目整体结构清晰：前端 `React+TS+Vite+Tailwind`、后端 `FastAPI+SQLAlchemy+Alembic+SQLite`、LLM 多 Provider 适配、SSE 流式输出、Prompt Studio/预设系统。  
但存在若干会在真实使用中出现的“隐蔽运行问题/可观测性误导/边界条件 bug”，以及大量可维护性债务（重复逻辑、超大文件、弱类型/宽泛异常捕获、预览接口副作用等）。

### 必须优先处理（P0）

1. **后端中间件对异常的日志记录会把业务异常也当成 500**，造成错误告警与排障误导，并产生重复日志。  
   - 位置：`backend/app/main.py:96`（`request_id_and_logging_middleware` 的 `except Exception` 分支）
2. **章节流式生成（`plan_first=true`）阶段若计划生成/提示词渲染报错，SSE 连接会“提前断开”，前端收到 `SSE_EARLY_CLOSE`**（非常难用正常操作复现与定位）。  
   - 位置：`backend/app/api/routes/chapters.py:946` ~ `backend/app/api/routes/chapters.py:1018`（`generate_chapter_stream` 中，计划生成/渲染逻辑在大 `try:` 之外）

---

## 1. 审查范围与覆盖说明

### 1.1 全量文件清单（用于“没有漏文件/漏代码行”的自证）

见 **附录 A：全量文本文件索引（164 个）**。该索引包含：文件行数、字节数、相对路径。

> 注：`.db.bak` 属于二进制/数据文件，不纳入“逐行审查代码”范围，但会在“仓库卫生/数据泄露风险”中指出。

### 1.2 本次重点关注维度

- **可靠性**：事务边界、SSE/长连接异常、并发/SQLite 约束、错误处理与回滚
- **安全**：API Key 明文泄露（日志/响应/导出/前端展示）、导出文件名注入、潜在 XSS
- **可维护性**：重复逻辑、超大函数/文件、隐式约定、弱类型/无约束 JSON、死代码
- **性能**：N+1 API 调用、频繁渲染、无必要网络往返
- **UI/UX**：交互一致性、错误反馈可读性、无障碍（ARIA/焦点/ESC）

---

## 2. 架构与实现优点（保留项）

1. **后端日志结构化**：`app/core/logging.py` 使用 JSON 单行日志，配合 `request_id` 有利于链路追踪。  
2. **LLM Key 红线意识**：`backend/README.md` 与 `app/core/logging.py` 主动压制 `httpx/httpcore` 日志，`app/llm/client.py` 具备 `_redact()` 逻辑。  
3. **SSE 基础设施清晰**：`backend/app/utils/sse_response.py` 和前端 `frontend/src/services/sseClient.ts` 各自封装了协议层。  
4. **Prompt Engine 具备可扩展性**：`prompt_presets + prompt_blocks + budget/override/injection` 的思路正确，足以支撑毕业设计 MVP 的迭代。

---

## 3. 重大问题清单（按严重级别）

### P0-01（后端）异常日志误报 500 + 重复记录

- **位置**
  - `backend/app/main.py:96`（`request_id_and_logging_middleware` 捕获所有 `Exception` 并写死 `status_code=500`）
  - `backend/app/main.py:125`（`@app.exception_handler(AppError)` 也会再次记录同一次异常）
- **问题描述**
  - 当前中间件 `except Exception` 会把 **任何**异常（包括 `AppError`、参数校验异常等）都记录为 `UNHANDLED_EXCEPTION` 且 `status_code=500`。
  - FastAPI 之后还会执行对应的异常处理器（`AppError` / `RequestValidationError` / `SQLAlchemyError` / `Exception`），导致**一次错误产生两条日志**，且第一条日志的 `status_code` 错误。
- **影响**
  - 监控/告警误报：业务 400/404 会被记为 500。
  - 排障误导：误以为是服务端崩溃/未处理异常。
  - 日志噪音：重复日志增大排查成本。
- **建议整改**
  - 方案 A（推荐）：中间件只做 request_id 注入与成功请求的 access log；异常统一由异常处理器记录。
  - 方案 B：中间件 `except` 分支对 `AppError` / `RequestValidationError` 单独处理并使用真实状态码（仍需避免与 handler 重复记录）。
- **验收标准**
  - 构造一次 `AppError.not_found()`：日志只出现 1 条，`status_code=404`。
  - 构造一次参数校验失败：日志只出现 1 条，`status_code=400`，且 payload 与日志一致。
  - 构造一次未知异常：日志只出现 1 条，`status_code=500`，并携带 `request_id`。

---

### P0-02（后端→前端）章节流式生成在 `plan_first` 阶段可能“无错误帧直接断流”

- **位置**
  - `backend/app/api/routes/chapters.py:946`（`if body.plan_first:` 开始）
  - `backend/app/api/routes/chapters.py:952`（`plan_result = call_llm_and_record(...)`）
  - `backend/app/api/routes/chapters.py:993`（渲染章节提示词）
  - `backend/app/api/routes/chapters.py:1018`（**大 `try:` 从这里才开始**，只覆盖“调用模型/解析/润色/返回”阶段）
  - 前端表现：`frontend/src/services/sseClient.ts:195`（抛 `SSE_EARLY_CLOSE`）
- **问题描述**
  - `generate_chapter_stream` 的 SSE 生成器内部：**计划生成**与**章节提示词渲染**发生在 `try: ... except AppError ...` 之外。
  - 若上述阶段抛 `AppError`（例如 prompt 配置缺失、LLM 配置缺失、LLM 调用失败），生成器会直接异常退出，**不会**发送 `sse_error + sse_done`，导致前端只能感知“连接提前结束”。
- **影响**
  - 真实用户体验：页面提示“流式连接提前结束”，缺少后端返回的明确错误信息/错误码。
  - 隐蔽性强：只有在 `plan_first=true` 且计划阶段失败时触发；常规手动流程很难稳定复现。
  - 生成记录不完整：异常发生在写入 `generation_runs` 之前时可能缺少审计记录。
- **建议整改**
  - 将 `plan_first` 阶段逻辑纳入同一个 `try/except` 块：任何 `AppError` 都必须 `yield sse_error(...); yield sse_done()`。
  - 明确“阶段”概念：在 SSE 里输出 `progress` 时附带 `stage`（例如 `stage: plan|render|call|parse|post_edit`），便于前端定位卡点。
  - 可选：在 `plan_first` 失败时也写入 `generation_runs`（run_type 例如 `plan_chapter_stream` 或沿用 `plan_chapter`）。
- **验收标准**
  - 删除/清空 `plan_chapter` 提示块后触发流式生成：前端必须收到 `type=error` 的 SSE 帧（不是 `SSE_EARLY_CLOSE`）。
  - `plan_first=true` 且 LLM Key 缺失：必须收到 SSE error，且带 `code=401`。
  - `plan_first=true` 且 LLM 上游 429/401：必须收到 SSE error，前端展示友好提示。

---

## 4. 高优先级问题（P1）

### P1-01（后端）PromptBlock 重新排序接口缺少“全量/去重”校验，可能造成排序数据悄然损坏

- **位置**
  - `backend/app/api/routes/prompts.py:263`（`reorder_prompt_blocks`）
  - `backend/app/api/routes/prompts.py:282`（仅遍历 `ordered_block_ids` 并赋值）
- **问题描述**
  - 仅校验 `ordered_block_ids` 中的 id 是否属于 preset，但没有校验：
    - 是否包含 preset 的 **全部 block**
    - 是否存在 **重复 id**
  - 若前端 bug/拖拽中断导致 `ordered_block_ids` 丢失或重复，后端会部分更新 injection_order，产生 **重复 order / 排序异常**，且难以追查。
- **建议整改**
  - 服务端增加强校验：`set(ordered_block_ids)==set(existing_block_ids)` 且长度一致，否则 400。
  - 事务内一次性更新，并在更新后返回稳定排序结果。
- **验收标准**
  - 传入缺少某个 block 的 ordered 列表：返回 400 且明确错误信息。
  - 传入包含重复 id：返回 400。

---

### P1-02（后端）Prompt Preview 端点可能产生 DB 副作用（预览触发写库）

- **位置**
  - `backend/app/services/prompt_presets.py:783`（`get_active_preset_for_task` 兜底创建 preset）
  - `backend/app/api/routes/prompts.py:330`（`preview_prompt` 调用 `render_preset_for_task`）
- **问题描述**
  - `render_preset_for_task()` 在找不到 preset 时会创建一个“Auto-created” preset 并 `commit`。
  - `preview_prompt` 是“预览”语义，但现在可能触发写入（尤其当 `task` 传入未知值时）。
- **影响**
  - 数据污染：用户只是预览，却悄悄多了 preset。
  - 调试困难：出现“莫名其妙的 preset”。
- **建议整改**
  - 方案 A：预览接口只允许白名单 task；未知 task 直接 400。
  - 方案 B：`render_preset_for_task` 增加参数 `allow_autocreate: bool = True`，预览传 `False`。
- **验收标准**
  - 调用 preview 时不会产生任何 DB 写入（可通过事务/审计/日志验证）。

---

### P1-03（后端）LLM 配置的默认 `max_tokens=32000` 对 Anthropic/Gemini 可能直接导致 400

- **位置**
  - `backend/app/api/routes/llm_preset.py:24`（默认 preset）
  - `backend/app/api/routes/projects.py:96`（绑定 profile 时创建 preset）
  - `backend/app/api/routes/llm_profiles.py:64`（profile 影响 project preset）
- **问题描述**
  - `max_tokens=32000` 是 OpenAI 生态常见默认，但 Anthropic/Gemini 常见上限更低（且模型差异大）。
  - 当前 `call_llm()` 对 Anthropic/Gemini **没有**“兼容降级序列”，会直接报 `LLM_BAD_REQUEST`。
- **建议整改**
  - 默认值改为 provider-aware：例如 `openai/openai_compatible=32000`，`anthropic/gemini=8192`（或更保守）。
  - 或者在 `prepare_llm_call()` 时对 provider 做 clamp。
- **验收标准**
  - 新建项目选择 `anthropic/gemini` 后，不手动改参数也能通过 `/api/llm/test`。

---

### P1-04（后端）API Key 解析逻辑大量重复，易导致未来修 bug 时“改一处漏三处”

- **位置（重复块示例）**
  - `backend/app/api/routes/chapters.py:246`、`backend/app/api/routes/chapters.py:431`、`backend/app/api/routes/chapters.py:788`
  - `backend/app/api/routes/outline.py:125`、`backend/app/api/routes/outline.py:277`
  - `backend/app/api/routes/llm.py:28`
- **问题描述**
  - 逻辑基本一致：优先使用 `X-LLM-API-Key`，否则从 `project.llm_profile_id -> LLMProfile.api_key_ciphertext -> decrypt_secret` 获取。
  - 重复块多处存在，未来新增 provider/错误码/脱敏规则时容易出现行为不一致。
- **建议整改**
  - 抽取为统一函数，例如 `app/services/llm_key_resolver.py::resolve_api_key(...)` 或 `app/api/deps.py` 提供依赖。
  - 统一错误码/错误信息与状态码。
- **验收标准**
  - 任意一个端点的 Key 解析行为变更（例如更严格空白处理）可以通过修改单点完成，全站一致。

---

### P1-05（前端）Dashboard 的“向导完成度”计算存在明显 N+1 网络问题

- **位置**
  - `frontend/src/pages/DashboardPage.tsx:47`（对每个项目依次请求 settings/characters/outline/chapters/llm_preset）
- **问题描述**
  - 每个项目 5 个请求 + 全局 profiles 1 个请求。项目数增大后会明显变慢，且后端压力倍增。
- **建议整改**
  - 方案 A：后端新增聚合端点（推荐）：`GET /api/projects/summary` 一次返回所有项目的向导所需字段/计数。
  - 方案 B：Dashboard 只在卡片进入可视区时懒加载（IntersectionObserver）。
  - 方案 C：只展示“最近 N 个项目”或分页。
- **验收标准**
  - 10 个项目场景下 Dashboard 首屏请求数显著下降（目标：<=3 个请求）。

---

## 5. 中优先级问题（P2：可维护性/体验/一致性）

### P2-01（后端）死代码/无用导入（会积累噪音，破坏可读性）

- **位置**
  - `backend/app/api/routes/outline.py:15`：导入 `call_llm_stream` 但未使用
  - `backend/app/api/routes/chapters.py:17`：导入 `call_llm_stream` 但未使用
  - `backend/app/services/output_parsers.py:265`：`parse_plan_output()` 未被引用
  - `backend/app/services/prompting.py:123`：`extract_json_object()` 未被引用
  - `backend/app/services/output_parsers.py:12`、`backend/app/services/output_parsers.py:13`：`CONTENT_MARKER/SUMMARY_MARKER` 未被引用
- **建议整改**
  - 移除无用导入/无用函数/无用常量；在 CI 中强制 lint（Python 建议引入 ruff；前端已有 eslint）。
- **验收标准**
  - lint 规则开启后无 “unused import/unused function”。

---

### P2-02（前端）SSE 客户端存在多处“`void x;`”式的可读性噪音，且没有主动取消 reader

- **位置**
  - `frontend/src/services/sseClient.ts:146`（`void eventType;`）
  - `frontend/src/services/sseClient.ts:177`（收到 done 直接 return，未显式 `reader.cancel()`）
- **建议整改**
  - 不使用的变量不要生成；或真正使用 `event:`（例如后端未来扩展 event 类型时可用）。
  - 在收到 `done` 或发生错误时调用 `reader.cancel()` / `response.body?.cancel()`，减少资源占用。
- **验收标准**
  - 频繁开始/取消生成后，浏览器 Network 面板无大量悬挂连接。

---

### P2-03（前端）章节流式解析依赖严格 marker，容错策略与后端解析不一致

- **位置**
  - `frontend/src/pages/WritingPage.tsx:460`（`markerContent/markerSummary` + `indexOf` 解析）
- **问题描述**
  - 后端解析（`backend/app/services/output_parsers.py:130`）允许 marker 漂移（空格/不完整等），而前端用严格 `indexOf("<<<CONTENT>>>")`。
  - 目前通过 “800 chars 后 fallback raw” 缓解，但会导致内容/摘要被错误拼接或 marker 被当正文。
- **建议整改**
  - 把 marker 解析逻辑抽成与后端一致的正则/状态机（甚至直接复刻后端 tolerant regex）。
  - 或者：后端 SSE 在 `result` 中总是返回结构化 `content_md/summary`，前端仅做展示，不做解析。
- **验收标准**
  - 模型输出 `<<< CONTENT >>>`、`<<<CONTENT` 等轻微漂移时，前端仍可正确拆分正文/摘要。

---

### P2-04（前端）导出 PromptPreset 的文件名未做系统兼容性处理

- **位置**
  - `frontend/src/pages/PromptStudioPage.tsx:484`（`a.download = \`${selectedPreset.name}.json\``）
- **问题描述**
  - preset 名称可能包含 `/:*?"<>|` 等字符，在 Windows 下会导致下载文件名异常。
- **建议整改**
  - 复用后端 `_safe_filename` 思路：前端增加 `sanitizeFilename()`（项目里已在 `apiClient.ts` 实现过一版，可复用）。
- **验收标准**
  - preset 名为 `A/B:C*D?` 时导出仍能得到可用文件名。

---

### P2-05（前端）`useWizardProgress` 的 `void version;` 属于可读性噪音

- **位置**
  - `frontend/src/hooks/useWizardProgress.ts:55`
- **建议整改**
  - 删除该行；若需要强制 useMemo 重新计算，只需依赖数组包含 `version` 即可（当前已包含）。

---

### P2-06（仓库卫生）提交了疑似运行产物/数据备份文件

- **位置**
  - `backend/ainovel.db.bak.20251218173544`（数据备份；不在 `.gitignore` 覆盖范围）
  - `backend/.dev-backend.err.log`、`backend/.dev-backend.out.log`（日志文件，即便是 0 字节也建议不纳入仓库）
- **风险**
  - 数据文件可能包含真实内容；不应进入 Git。
- **建议整改**
  - 将 `*.db.bak*`、`.dev-backend.*.log` 加入 `.gitignore`；从 Git 历史清理（必要时用 BFG/Filter-Repo）。
- **验收标准**
  - `git status` 不再出现运行产物；仓库不包含真实 DB。

---

## 6. 可执行的“重构任务拆分”（按模块编号，可逐项验收）

> 说明：下面每个任务都尽量拆到“一个 PR 可完成”的颗粒度，并给出验收标准。  
> 建议执行顺序：先 P0 → 后端可观测性 → SSE 稳定性 → 业务一致性 → 维护性 → 性能/UI。

### 后端（BE）任务

- [ ] **BE-P0-01 修正异常日志与 request middleware 责任边界**  
  - 涉及：`backend/app/main.py:89`  
  - 验收：见 P0-01

- [ ] **BE-P0-02 修复章节流式生成 `plan_first` 阶段异常不落 SSE error 的问题**  
  - 涉及：`backend/app/api/routes/chapters.py:946`、`backend/app/api/routes/chapters.py:1018`  
  - 验收：见 P0-02

- [ ] **BE-P1-01 PromptBlock reorder 全量/去重校验**  
  - 涉及：`backend/app/api/routes/prompts.py:263`  
  - 验收：见 P1-01

- [ ] **BE-P1-02 Prompt preview 禁止写库（去副作用）**  
  - 涉及：`backend/app/api/routes/prompts.py:330`、`backend/app/services/prompt_presets.py:783`  
  - 验收：见 P1-02

- [ ] **BE-P1-03 Provider-aware 默认参数（尤其 max_tokens）**  
  - 涉及：`backend/app/api/routes/llm_preset.py:16`、`backend/app/api/routes/projects.py:96`、`backend/app/api/routes/llm_profiles.py:64`  
  - 验收：见 P1-03

- [ ] **BE-P1-04 统一 API Key 解析逻辑（抽函数 + 全端点替换）**  
  - 涉及：`backend/app/api/routes/chapters.py`、`backend/app/api/routes/outline.py`、`backend/app/api/routes/llm.py`  
  - 验收：见 P1-04

- [ ] **BE-P2-01 清理无用导入/死代码**  
  - 涉及：`backend/app/api/routes/outline.py:15`、`backend/app/api/routes/chapters.py:17`、`backend/app/services/output_parsers.py:265`、`backend/app/services/prompting.py:123`  
  - 验收：删除后运行 lint/静态检查无告警

- [ ] **BE-P2-02 收紧 JSON 解析异常捕获范围（替换 `except Exception` → `except json.JSONDecodeError`）**  
  - 涉及：`backend/app/api/routes/generation_runs.py:42` 等  
  - 验收：错误分支仍兼容 `_raw` 兜底；异常不吞其它错误

- [ ] **BE-P2-03 启动迁移策略梳理（生产与开发分流）**  
  - 涉及：`backend/app/db/migrations.py:42`  
  - 验收：`APP_ENV=prod` 时不自动 stamp；迁移策略有文档且可回滚

### 前端（FE）任务

- [ ] **FE-P1-01 Dashboard 向导完成度聚合（消除 N+1）**  
  - 涉及：`frontend/src/pages/DashboardPage.tsx:47`  
  - 验收：首屏请求数显著下降（<=3）

- [ ] **FE-P2-01 SSEClient 主动 cancel reader + 去掉无意义 `void`**  
  - 涉及：`frontend/src/services/sseClient.ts:146`、`frontend/src/services/sseClient.ts:177`  
  - 验收：频繁生成/取消无悬挂连接

- [ ] **FE-P2-02 WritingPage 流式 marker 解析与后端契约对齐**  
  - 涉及：`frontend/src/pages/WritingPage.tsx:460`  
  - 验收：marker 漂移仍可正确拆分正文/摘要

- [ ] **FE-P2-03 PromptStudio 导出文件名 sanitize**  
  - 涉及：`frontend/src/pages/PromptStudioPage.tsx:484`  
  - 验收：Windows/macOS 下文件名稳定

- [ ] **FE-P2-04 移除 `useWizardProgress` 的可读性噪音**  
  - 涉及：`frontend/src/hooks/useWizardProgress.ts:55`

### 工程/仓库（INF）任务

- [ ] **INF-01 清理数据/日志产物并完善 `.gitignore`**  
  - 涉及：`.gitignore`、`backend/ainovel.db.bak.20251218173544`、`backend/.dev-backend.*.log`  
  - 验收：仓库不包含真实 DB/日志产物

- [ ] **INF-02 依赖可复现性（Python 依赖建议锁定）**  
  - 涉及：`backend/requirements.txt`  
  - 建议：引入 `pip-tools` 或 `uv` lock（按团队习惯）

---

## 7. 附录 A：全量文本文件索引（164）

> 格式：`行数  字节数  相对路径`

```
   106      4141 CLAUDE.md
    77      2355 README.md
  2876     96570 ainovel开发计划_v2.md
    38       603 backend/alembic.ini
    69      1831 backend/alembic/env.py
    27       534 backend/alembic/script.py.mako
   176      8413 backend/alembic/versions/0f24b611cf21_init.py
   192      8236 backend/alembic/versions/1c2a0e6b4c2d_multi_outlines_and_llm_profiles.py
   288     11430 backend/alembic/versions/2c72bcfbae89_import_prompt_templates_into_prompt_.py
    32       805 backend/alembic/versions/2f6b9f4a0c1e_drop_llm_profiles_api_key.py
    31       837 backend/alembic/versions/3d1c1e8a9f0b_add_llm_profiles_api_key_ciphertext.py
    73      3124 backend/alembic/versions/d8993729ae5a_add_prompt_presets_and_blocks.py
    28       698 backend/alembic/versions/f078e253d338_add_generation_runs_prompt_render_log_.py
     1         2 backend/app/__init__.py
     1         2 backend/app/api/__init__.py
    71      2373 backend/app/api/deps.py
    35      1173 backend/app/api/router.py
     1         2 backend/app/api/routes/__init__.py
  1232     54102 backend/app/api/routes/chapters.py
    71      2717 backend/app/api/routes/characters.py
   130      4578 backend/app/api/routes/export.py
   116      3801 backend/app/api/routes/generation_runs.py
    15       401 backend/app/api/routes/health.py
    83      3590 backend/app/api/routes/llm.py
   120      3983 backend/app/api/routes/llm_preset.py
   157      5874 backend/app/api/routes/llm_profiles.py
   528     23328 backend/app/api/routes/outline.py
   146      5100 backend/app/api/routes/outlines.py
   132      5658 backend/app/api/routes/projects.py
   421     16029 backend/app/api/routes/prompts.py
    58      2222 backend/app/api/routes/settings.py
     1         2 backend/app/core/__init__.py
    57      1742 backend/app/core/config.py
    37      1394 backend/app/core/errors.py
    32      1064 backend/app/core/logging.py
    19       392 backend/app/core/request_id.py
   155      4756 backend/app/core/secrets.py
     1         2 backend/app/db/__init__.py
     6        90 backend/app/db/base.py
    67      2274 backend/app/db/migrations.py
    47      1246 backend/app/db/session.py
    13       284 backend/app/db/utils.py
     1         2 backend/app/llm/__init__.py
  1017     44456 backend/app/llm/client.py
    30       654 backend/app/llm/http_client.py
    66      2081 backend/app/llm/messages.py
    15       558 backend/app/llm/utils.py
   184      6298 backend/app/main.py
    25       721 backend/app/models/__init__.py
    28      1339 backend/app/models/chapter.py
    23       896 backend/app/models/character.py
    30      1587 backend/app/models/generation_run.py
    27      1291 backend/app/models/llm_preset.py
    25      1155 backend/app/models/llm_profile.py
    22       903 backend/app/models/outline.py
    31      1263 backend/app/models/project.py
    19       599 backend/app/models/project_settings.py
    33      1750 backend/app/models/prompt_block.py
    24      1028 backend/app/models/prompt_preset.py
    19       743 backend/app/models/user.py
     1         2 backend/app/schemas/__init__.py
     9       197 backend/app/schemas/base.py
    23       768 backend/app/schemas/chapter_generate.py
    11       332 backend/app/schemas/chapter_plan.py
    48      1171 backend/app/schemas/chapters.py
    29       750 backend/app/schemas/characters.py
    24       451 backend/app/schemas/common.py
    23       646 backend/app/schemas/generation_runs.py
     5       145 backend/app/schemas/llm.py
    38      1206 backend/app/schemas/llm_preset.py
    32      1065 backend/app/schemas/llm_profiles.py
    17       554 backend/app/schemas/llm_test.py
    35       767 backend/app/schemas/outline.py
    16       423 backend/app/schemas/outline_generate.py
    31       967 backend/app/schemas/projects.py
   146      5005 backend/app/schemas/prompt_presets.py
    17       358 backend/app/schemas/settings.py
     1         2 backend/app/services/__init__.py
   216      6873 backend/app/services/generation_service.py
    17       483 backend/app/services/length_control.py
    27       691 backend/app/services/outline_store.py
    82      3304 backend/app/services/output_contracts.py
   270     10196 backend/app/services/output_parsers.py
    46      1114 backend/app/services/prompt_budget.py
  1071     42498 backend/app/services/prompt_presets.py
    21       639 backend/app/services/prompt_store.py
   138      4416 backend/app/services/prompting.py
    53      1647 backend/app/services/run_store.py
     2        42 backend/app/utils/__init__.py
    71      1903 backend/app/utils/sse_response.py
     9       229 backend/requirements.txt
     6        96 frontend/.prettierrc.json
    73      2632 frontend/README.md
    23       646 frontend/eslint.config.js
    31      1073 frontend/index.html
  5733    211128 frontend/package-lock.json
    43      1161 frontend/package.json
     6        87 frontend/postcss.config.js
     1      1497 frontend/public/vite.svg
    42       648 frontend/src/App.css
    57      2256 frontend/src/App.tsx
     1      4126 frontend/src/assets/react.svg
    23       786 frontend/src/components/atelier/GhostwriterIndicator.tsx
    86      3313 frontend/src/components/atelier/MarkdownEditor.tsx
    46      1350 frontend/src/components/atelier/ProjectSwitcher.tsx
    45      1627 frontend/src/components/atelier/ThemeToggle.tsx
   242      8854 frontend/src/components/atelier/WizardNextBar.tsx
   380     15232 frontend/src/components/layout/AppShell.tsx
    23       726 frontend/src/components/layout/ProjectProviderGuard.tsx
   311     11916 frontend/src/components/prompts/LlmPresetPanel.tsx
    16       350 frontend/src/components/prompts/types.ts
    84      3270 frontend/src/components/ui/ConfirmProvider.tsx
    68      2157 frontend/src/components/ui/Drawer.tsx
    37      1196 frontend/src/components/ui/Modal.tsx
    35      1019 frontend/src/components/ui/Overlay.tsx
   103      3778 frontend/src/components/ui/ToastProvider.tsx
    29       796 frontend/src/components/ui/confirm.ts
    14       456 frontend/src/components/ui/toast.ts
   271     10639 frontend/src/components/writing/AiGenerateDrawer.tsx
    90      2964 frontend/src/components/writing/CreateChapterDialog.tsx
   148      6360 frontend/src/components/writing/GenerationHistoryDrawer.tsx
    36       851 frontend/src/components/writing/types.ts
    28       981 frontend/src/contexts/ProjectsContext.tsx
    17       482 frontend/src/contexts/projects.ts
    54      1474 frontend/src/hooks/useProjectData.ts
    15       511 frontend/src/hooks/useSaveHotkey.ts
    34      1021 frontend/src/hooks/useUnsavedChangesGuard.tsx
    77      2877 frontend/src/hooks/useWizardProgress.ts
   187      5214 frontend/src/index.css
    30       886 frontend/src/lib/motion.ts
    10       245 frontend/src/main.tsx
   286     10677 frontend/src/pages/CharactersPage.tsx
   300     12053 frontend/src/pages/DashboardPage.tsx
   174      6501 frontend/src/pages/ExportPage.tsx
    12       351 frontend/src/pages/NotFoundPage.tsx
   825     32345 frontend/src/pages/OutlinePage.tsx
   181      7073 frontend/src/pages/PreviewPage.tsx
   358     14018 frontend/src/pages/ProjectWizardPage.tsx
  1138     45741 frontend/src/pages/PromptStudioPage.tsx
   617     21118 frontend/src/pages/PromptsPage.tsx
   218      8514 frontend/src/pages/SettingsPage.tsx
  1028     40630 frontend/src/pages/WritingPage.tsx
   163      5016 frontend/src/services/apiClient.ts
     8       273 frontend/src/services/currentUser.ts
   199      7011 frontend/src/services/sseClient.ts
     8       308 frontend/src/services/storageKeys.ts
    35       976 frontend/src/services/theme.ts
     9       302 frontend/src/services/uiState.ts
   233      8205 frontend/src/services/wizard.ts
   144      3177 frontend/src/types.ts
    34      1142 frontend/tailwind.config.js
    28       760 frontend/tsconfig.app.json
     4       111 frontend/tsconfig.json
    26       679 frontend/tsconfig.node.json
    15       316 frontend/vite.config.ts
  1578     68098 mvp开发计划.md
   678     26282 mvp改进建议.md
   484     35373 mvp最终改善文档.md
   261     18583 temp-process.md
   305     13757 ui设计规范.md
   186     17511 对话交接.md
   187     18534 开发进度.md
  1134     49508 提示词系统实现计划.md
  1691     54436 更进一步实现.md
```

