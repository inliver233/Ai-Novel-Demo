# 代码质量优化报告（ainovel / `new-demo/demo`）

> 评审日期：2026-01-03  
> 评审基线：`git HEAD = 4d775df698d70d34feeb8d0070d5893ef27c88db`（当前工作区存在**未提交改动**与**未跟踪源码**，见「P0-01」）  
> 评审目标：以“大厂生产级”标准对**后端 FastAPI + 前端 React/TS**进行可维护性、稳定性、安全性、可测试性、可观测性、UI/UX 一致性等全方位审计，并给出**可拆分、可逐步验收**的整改任务清单。  

---

## 0. 评审方法与校验结果（可复现）

### 0.1 评审范围（按仓库代码）

本次审计覆盖：
- 后端：`backend/app/**`、`backend/alembic/**`、`backend/scripts/**`、`backend/tests/**`、`backend/requirements*.txt`、`backend/alembic.ini`
- 前端：`frontend/src/**`、`frontend/package.json`、`frontend/eslint.config.js`、`frontend/tsconfig*.json`、`frontend/vite.config.ts`、`frontend/vitest.config.ts`
- 仓库工程卫生：根目录 `README.md`、`.gitignore`、`.gitattributes`

明确不纳入逐行评审的内容（因为属于第三方/产物/体积不可控）：
- `frontend/node_modules/**`、`frontend/dist/**`
- `backend/.venv/**`
- 本地 DB 文件：`**/*.db`、`**/*.db-wal`、`**/*.db-shm` 等（但会从“工程卫生/风险”角度提出规范）

> 重要：虽然本报告以 `git ls-files` 的“被追踪文件”为主，但当前工作区存在若干 `??` 未跟踪的 **TypeScript 源码**，且被追踪文件直接 import 使用（会导致“提交后构建必挂”）。这些未跟踪源码已纳入「P0-01」与后续任务清单。

### 0.2 已执行的自动化校验（本地）

后端：
- `python -m compileall -q backend/app`
- `PYTHONPATH=backend python -m unittest discover -s backend/tests -p "test_*.py" -v`（通过，16 tests OK）

前端：
- `npm test`（`vitest run`，通过）
- `npm run lint`（eslint + prettier --check，通过）
- `npx tsc -p tsconfig.app.json`（通过）

联网核对（用于“API/最佳实践是否过时”的风险判断）：
- OpenAI 官方 deprecations 页面（用于判断 Responses API / Assistants 迁移趋势）
- Anthropic Streaming / Messages API（用于判断 SSE/stream 支持）
- Google Gemini API 文档（用于判断 key 传递方式与 stream endpoint）

### 0.3 当前工作区状态（必须先说明，否则行号/结论无法复现）

`git status --porcelain -uall`（评审时刻）要点：
- 已修改（M）：`README.md`、后端 `backend/app/api/routes/{chapter_analysis,chapters,outline}.py`、`backend/app/utils/sse_response.py`；前端 `frontend/src/App.tsx`、`frontend/src/pages/WritingPage.tsx`、`frontend/src/pages/writing/useBatchGeneration.ts`、`frontend/src/services/{apiClient,sseClient}.ts` 等。
- 未跟踪（??）但被追踪代码 import 使用：`frontend/src/pages/writing/useApplyGenerationRun.ts`、`useChapterCrud.ts`、`useChapterEditor.ts`、`useChapterGeneration.ts`、`useOutlineSwitcher.ts`、`writingErrorUtils.ts`、`writingErrorUtils.test.ts`（详见「P0-01」）。

> 行号说明：本报告引用的 `path:line` 均以**当前工作区文件内容**为准（不是历史 commit 的行号）。整改过程中如改动了文件结构，请用“搜索关键词”重新定位。

---

## 1. 结论摘要（Executive Summary）

### 1.1 最重要的结论（先说人话）

1) **项目整体质量在“Demo/MVP”维度是能跑通的**：有统一的错误结构（`ok_payload/error_payload`）、请求 ID、LLM 调用审计（generation_runs）、Prompt Studio 与安全模板子集、前后端均有基础测试与 lint。

2) **离“生产级”差的不是单点 bug，而是“工程化与边界设计”**：主要集中在
- **源码与 Git 状态不一致（P0）**：追踪文件引用了未跟踪源码，任何 CI/部署都会直接失败；
- **LLM Provider 适配长期维护风险（P0/P1）**：Gemini key 通过 query 传递、OpenAI API 生态在 2025-2026 有明显迁移趋势、各 Provider 的 stream/param 差异需要模块化；
- **可维护性风险（P1）**：少数超大文件（后端 `prompt_presets.py`/`llm/client.py`、前端 `PromptStudioPage.tsx`）带来迭代成本飙升与回归风险；
- **并发/一致性风险（P1）**：前端多处缺少请求取消与竞态保护；后端 BackgroundTasks 的批量任务机制不具备生产可靠性。

### 1.2 本次审计的“阻断项”（P0）一览

- P0-01：追踪源码 import 未跟踪源码（提交后构建/部署必挂）
- P0-02：UI 文案异常（Suspense fallback 出现明显乱码/非预期语言）
- P0-03：Gemini API key 通过 URL query 传递（泄漏面扩大 + 与官方推荐不一致）

---

## 2. P0 阻断问题（必须先修，未修复前不建议继续加功能）

### P0-01：Git 状态不一致：追踪文件依赖未跟踪源码（构建/部署必挂）

**问题定位**
- `frontend/src/pages/WritingPage.tsx:20-27`：import 了 `./writing/useApplyGenerationRun`、`useChapterCrud`、`useChapterEditor`、`useChapterGeneration`、`useOutlineSwitcher` 等模块。
- 但这些文件在当前工作区为 **未跟踪**（`git status --porcelain -uall` 显示 `?? frontend/src/pages/writing/useApplyGenerationRun.ts` 等）。
- 另外：`frontend/src/pages/writing/useBatchGeneration.ts:7` import `./writingErrorUtils`，而 `writingErrorUtils.ts` 也是未跟踪。

**风险/影响**
- 任何“干净检出”的环境（CI、同事机器、部署机）会在 `vite build` 或 `tsc` 阶段直接报错：`Cannot find module './writing/useApplyGenerationRun'`。
- 这是**发布阻断**：不解决就无法进入“可复制、可交付”的工程状态。

**整改任务拆分（可逐步验收）**
- [ ] P0-01A：确认这些 `??` 文件是否应该纳入仓库  
  - 验收：负责人明确选择：**(1) git add 提交** 或 **(2) 回滚 WritingPage 的重构**（二选一，不允许“半成品”）。
- [ ] P0-01B：若选择提交：将未跟踪源码加入 git（至少包括）  
  - `frontend/src/pages/writing/useApplyGenerationRun.ts`  
  - `frontend/src/pages/writing/useChapterCrud.ts`  
  - `frontend/src/pages/writing/useChapterEditor.ts`  
  - `frontend/src/pages/writing/useChapterGeneration.ts`  
  - `frontend/src/pages/writing/useOutlineSwitcher.ts`  
  - `frontend/src/pages/writing/writingErrorUtils.ts`  
  - `frontend/src/pages/writing/writingErrorUtils.test.ts`  
  - 验收：`git status --porcelain -uall` 不再出现上述 `??`；`npm run lint && npm test && npx tsc -p tsconfig.app.json` 全通过。
- [ ] P0-01C：若选择回滚：将 `frontend/src/pages/WritingPage.tsx` 与 `frontend/src/pages/writing/useBatchGeneration.ts` 回退到“未依赖这些 hooks”的版本  
  - 验收：`git diff --stat` 中不再出现“引用未跟踪模块”；前端构建通过。

> 建议选择：**提交这些 hooks**。从结构上看这是一次“把 WritingPage 拆成 hooks”的重构方向，长期更利于维护，但必须完整交付。

---

### P0-02：UI 文案异常：Suspense fallback 出现明显乱码/非预期语言

**问题定位**
- `frontend/src/App.tsx:103`：`<Suspense fallback={<div className="p-6 text-subtext">Мгдижа...</div>}>`

**风险/影响**
- 这类文本通常来自误粘贴/编码错误/占位符未替换，会直接降低产品可信度；也会被误判为“i18n/编码混乱”。

**整改任务拆分**
- [ ] P0-02A：替换 fallback 文案为中文/统一术语（例如“加载中…”）  
  - 验收：本地访问任意懒加载页面（Dashboard/Writing/Prompt Studio 等）时 fallback 文案正确。
- [ ] P0-02B：建立“文本来源规范”  
  - 要求：UI 文案必须集中在一个地方（至少按模块集中），避免散落在各组件里出现混杂语言。
  - 验收：新增 `i18n` 前无需全量改造，但至少保证“新增文案不再随机散落”。

---

### P0-03：Gemini API key 通过 URL query 传递（泄漏面扩大 + 与官方推荐不一致）

**问题定位**
- `backend/app/llm/client.py:865`、`backend/app/llm/client.py:1162`：`url = f"{endpoint}?{urlencode({'key': api_key})}"`

**风险/影响**
- API key 出现在 URL 的 query 中，会进入更多日志面（代理、网关、浏览器历史、监控、WAF、反向代理 access log）；即便你做了脱敏，也很难保证全链路不泄漏。
- Gemini 官方文档推荐使用 `x-goog-api-key` header（而不是 query）。

**整改任务拆分**
- [ ] P0-03A：将 Gemini key 传递方式由 query 改为 header  
  - 修改点：`backend/app/llm/client.py` Gemini 分支（上述两处）。
  - 验收：对 Gemini 的请求不再包含 `?key=`；错误日志与 request log 不包含明文 key。
- [ ] P0-03B：为 Gemini client 增加 MockTransport 单测覆盖（参考 `backend/scripts/verify_anthropic_max_tokens_retry.py` 的写法）  
  - 验收：单测断言请求 headers 中含 `x-goog-api-key`，且 query 不包含 key。

---

## 3. P1 高优问题（不一定“立刻挂”，但会持续制造事故/返工）

### P1-01：请求上下文 `request_id` 未显式 reset，存在跨任务污染风险

**问题定位**
- `backend/app/main.py:89-109`：每个请求调用 `set_request_id(rid)`，但没有 reset。
- `backend/app/core/request_id.py:3-16`：`ContextVar` 仅 set/get，无 reset token。

**风险/影响**
- 在复杂并发场景（后台任务、线程池/异步上下文切换、异常路径）里，ContextVar 的值有机会“粘”在错误的执行上下文中，导致日志串号、排查困难。

**整改任务拆分**
- [ ] P1-01A：让 `set_request_id` 返回 token，并在 middleware `finally` reset  
  - 修改点：`backend/app/core/request_id.py`、`backend/app/main.py`。
  - 验收：新增单测（或最小可复现脚本）证明并发请求不会串 request_id。

---

### P1-02：LLM 适配器过于集中在单文件，重复逻辑多，长期维护成本极高

**问题定位**
- `backend/app/llm/client.py`（约 1251 行）：包含 openai/openai_compatible/anthropic/gemini 的同步与流式调用、兼容降级、错误映射、脱敏等。
  - 典型重复：`call_llm_stream` vs `call_llm_stream_messages`（`backend/app/llm/client.py:258` 与 `:449`）基本重复。
  - `extra = extra or {}` 在 stream 分支中赋值但未使用：`backend/app/llm/client.py:277`、`backend/app/llm/client.py:467`（死代码信号）。

**风险/影响**
- 每次加 provider / 改参数 / 修 bug 都可能引起跨 provider 回归，review 成本也会指数级上升。
- 这类“巨石文件”极容易出现“修 A 挂 B”的堆叠 bug。

**整改任务拆分**
- [ ] P1-02A：把 `backend/app/llm/client.py` 拆成 provider adapters（建议结构示例）  
  - `backend/app/llm/providers/openai_chat.py`  
  - `backend/app/llm/providers/anthropic_messages.py`  
  - `backend/app/llm/providers/gemini_generate_content.py`  
  - `backend/app/llm/client.py` 只保留统一接口与路由分发
  - 验收：原有单测 + 新增 MockTransport 单测覆盖 openai/anthropic/gemini 的关键分支（错误映射/降级/脱敏/stream）。
- [ ] P1-02B：消除重复的“降级步骤”实现，抽象成可复用函数  
  - 验收：重复代码行数显著下降；同一套 downgrade steps 覆盖 sync/stream。
- [ ] P1-02C：删除或真正使用 `extra`（stream 分支）  
  - 验收：`rg -n "extra = extra or" backend/app/llm/client.py` 不再出现“赋值不用”。

---

### P1-03：前端存在多处“请求竞态”隐患（快速切换时可能回填旧数据）

**问题定位（示例）**
- `frontend/src/hooks/useProjectData.ts:27-41`：`refresh()` 不做请求序列号/AbortController 防抖；projectId 快速切换会有“旧请求晚返回覆盖新状态”的风险。
- 未跟踪的写作 hooks（当前工作区存在）：`frontend/src/pages/writing/useChapterEditor.ts` 中加载章节也没有 abort/序列号（需要在纳入仓库后补齐测试与竞态保护）。
- `frontend/src/pages/writing/useGenerationHistory.ts:33-60`：快速切换 run 时也可能出现旧 run 覆盖。

**风险/影响**
- 用户快速切换项目/章节/大纲时，UI 会出现“突然跳回之前的数据”、表单被覆盖、dirty 状态异常等“难以稳定复现”的线上 bug。

**整改任务拆分**
- [ ] P1-03A：为 `useProjectData.refresh()` 增加竞态保护（request sequence 或 AbortController）  
  - 修改点：`frontend/src/hooks/useProjectData.ts`。
  - 验收：构造快速切换 projectId 的手工测试/自动测试，确认不会回填旧数据。
- [ ] P1-03B：对“章节加载/生成历史加载”等关键请求统一采用同一套竞态治理（建议封装 `useAbortableRequest` 或 `useRequestSeqGuard`）  
  - 验收：在 Writing 页面快速切换章节/大纲时，编辑器内容不会被旧请求覆盖。

---

### P1-04：批量生成任务依赖 FastAPI `BackgroundTasks`，生产可靠性不足

**问题定位**
- `backend/app/api/routes/batch_generation.py:147`：`background_tasks.add_task(run_batch_generation_task, task_id=task_id)`
- `backend/app/services/batch_generation_service.py:160-461`：后台顺序生成并写入 DB

**风险/影响**
- `BackgroundTasks` 只保证“在响应返回后执行”，但不保证：
  - 进程重启后任务可恢复
  - 多实例部署下任务不重复执行
  - 崩溃/超时/资源限制下任务可靠完成
- 这会在生产引入“任务丢失/重复/状态卡死”的事故。

**整改任务拆分**
- [ ] P1-04A：定义任务执行的“生产口径”  
  - 选择：Celery/RQ/Arq/自建队列（至少要有：持久化队列、重试、去重、可观测、可取消）。
  - 验收：文档+PoC，明确部署模型与幂等策略。
- [ ] P1-04B：将 `run_batch_generation_task` 抽象为“纯函数 Worker”并提供幂等保证  
  - 验收：同一个 `task_id` 被重复执行不会产生重复 run/item 状态错乱。

---

### P1-05：默认 Prompt 模板以“超大 Python 文件常量”形式维护，迭代风险与冲突成本极高

**问题定位**
- `backend/app/services/prompt_presets.py`（约 1844 行）：包含大量默认模板/块配置，属于“数据型内容塞进代码”的典型形态。

**风险/影响**
- merge 冲突高发（多人同时改提示词/块配置时几乎必冲突）
- 代码 review 极难（模板改动淹没业务逻辑改动）
- 版本化/灰度/多语言支持困难

**整改任务拆分**
- [ ] P1-05A：把默认 presets/blocks 抽为外部资源（JSON/YAML/Markdown），由 migration 或启动时导入  
  - 验收：`prompt_presets.py` 只保留“导入/升级逻辑”，不再包含大段模板文本。
- [ ] P1-05B：为模板资源引入版本号与升级策略（避免“启动时 ensure_* 写库”与“用户自定义修改”互相覆盖）  
  - 验收：升级时不覆盖用户修改；能明确区分“内置模板版本”与“项目自定义版本”。

## 4. P2 中优问题（建议迭代中修，避免技术债滚雪球）

### P2-01：默认 max_tokens 取值偏激进，靠“降级/截断”兜底会影响体验

**问题定位**
- `backend/app/llm/utils.py:14-23`：`default_max_tokens_for_provider` 对 openai/openai_compatible 返回 32000
- `backend/app/api/routes/llm_preset.py:16-31`：默认 preset `max_tokens=32000`
- `backend/app/services/length_control.py:2-18`：provider cap openai=32000，anthropic/gemini=8192

**风险/影响**
- 许多模型/网关会直接拒绝过大 max_tokens，当前策略是“失败后逐步 clamp/丢参”，会：
  - 增加一次生成的平均重试次数（更慢、更贵）
  - 让用户更难理解“为什么设置不生效”

**整改建议**
- [ ] P2-01A：将默认 max_tokens 改为更保守的值，并在 UI 提供“模型上下文/输出上限提示”
- [ ] P2-01B：引入“按 model 维度”的能力表（至少 openai 官方 + 常用兼容网关 + anthropic/gemini）
- [ ] P2-01C：把“降级/丢参”结果回传给前端并可视化（当前已部分支持 dropped_params）

---

### P2-02：时间字段以字符串存储，后期统计/排序/时区处理成本高

**问题定位（示例）**
- `backend/app/models/project.py:16-23`、`backend/app/models/outline.py:13-18`、`backend/app/models/generation_run.py:8-22` 等：`created_at/updated_at` 使用 `String(32)` 存储 ISO 文本。

**风险/影响**
- 数据库层无法做原生时间函数/索引优化（虽然 SQLite 还能凑合，但迁移到 Postgres 会更痛）

**整改建议**
- [ ] P2-02A：迁移为真正的 datetime（`DateTime(timezone=True)`），并在 Alembic 做向后兼容迁移
- [ ] P2-02B：前端/导出仍可按 ISO 输出，但 DB 内部存储用 datetime

---

### P2-03：前端输入数字使用 `Number(e.target.value)`，空值/非法值会变成 `0/NaN`

**问题定位**
- `frontend/src/components/writing/AiGenerateDrawer.tsx:98-108`：`target_word_count: Number(e.target.value)`

**风险/影响**
- 用户清空输入框时 `Number("") === 0`，会把“未设置”误当成 0；与后端 `target_word_count` 的 `ge=100` 约束交互不一致。

**整改建议**
- [ ] P2-03A：改用 `e.target.valueAsNumber` 并对 `NaN` 映射为 `0` 或 `null`（推荐 `null` 表示“未设置”）
- [ ] P2-03B：统一前端 GenerateForm 的语义：`null` 表示“交给后端默认估算”

---

## 5. 后端：文件级审计要点（含行号定位）

> 说明：这里列出“值得立刻改/容易踩坑/影响维护”的点；未列出的问题不代表完美，只代表“未发现显著风险或属于风格偏好”。

### 5.1 `backend/app/main.py`

- 问题：request_id ContextVar 未 reset（见 P1-01）  
  - 定位：`backend/app/main.py:89-109`、`backend/app/core/request_id.py:3-16`
- 建议：middleware 增加 `try/finally`，并记录 4xx 的访问日志（当前只记录 `<400`）  
  - 定位：`backend/app/main.py:99-107`

### 5.2 `backend/app/llm/client.py`

- 问题：provider 适配巨石文件，重复逻辑多（见 P1-02）  
  - 定位：`backend/app/llm/client.py:258`、`:449`、`:637`、`:919`
- 问题：Gemini key 通过 query 传递（见 P0-03）  
  - 定位：`backend/app/llm/client.py:865`、`:1162`
- 问题：stream 分支 `extra` 赋值未使用（死代码信号）  
  - 定位：`backend/app/llm/client.py:277`、`:467`
- 建议：为 anthropic/gemini 补齐 stream 支持（官方已支持），并统一 SSE 解析策略  
  - 定位：`backend/app/llm/client.py:272-273`（目前仅 openai/openai_compatible 支持 stream）

### 5.3 `backend/app/api/routes/prompts.py`

- 问题：`PromptBlockUpdate` 的 `budget/cache` 字段无法用 `null` 清空（只有传 `{}` 才会清空），语义不直观  
  - 定位：`backend/app/api/routes/prompts.py:238-241`
- 建议：所有“可选字段”更新逻辑统一使用 `model_fields_set` 判断，避免“传 null vs 不传”语义混乱  
  - 参考：`backend/app/api/routes/prompts.py:224-235`（template/marker_key/triggers 的写法）

### 5.4 `backend/app/api/routes/chapters.py`

- 问题：文件过大（约 1000+ 行）且混合 CRUD + 生成 + SSE，未来维护成本高  
  - 定位：`backend/app/api/routes/chapters.py` 全文件
- 问题：流式生成进度用 `len(raw_output)`（字符数）对比 `target_word_count`（字数/词数）进行估算，存在明显语义偏差  
  - 定位：`backend/app/api/routes/chapters.py:875-883`
- 建议：把“生成/规划/润色”的流程抽成 service 层统一 pipeline（你已经有 `generation_pipeline.py`，但 `chapters.py` 仍然保留大量流程细节）  
  - 定位：`backend/app/api/routes/chapters.py:697-1076` 与 `backend/app/services/generation_pipeline.py`

### 5.5 `backend/app/api/routes/outline.py`

- 优点：提供 parse_error 时的“修复 JSON”二次调用（实用）  
  - 定位：`backend/app/api/routes/outline.py:196-225`
- 建议：把“fix json”能力抽到通用层（不仅 outline_generate 需要）并补齐单测  
  - 定位：`backend/app/services/output_contracts.py:66-80`

### 5.6 `backend/app/schemas/llm_profiles.py`

- 问题：provider 仅为 `str`，未复用 `LLMProvider`，导致非法 provider 能通过 schema 并推迟到运行时报错  
  - 定位：`backend/app/schemas/llm_profiles.py:6-32`
- 建议：将 provider 类型改为 `app.schemas.llm.LLMProvider`，与 `llm_preset/llm_test` 一致。

### 5.7 `backend/app/services/prompt_presets.py`

- 问题：超大文件（见 P1-05），且承担“默认数据定义 + 导入写库”双重职责  
  - 定位：`backend/app/services/prompt_presets.py` 全文件
- 建议：把模板视为“数据资产”，与应用代码解耦；同时补齐模板变更的自动化校验（例如：模板渲染不缺变量、token 预算不超、输出 contract 能被 parser 解析）。

---

## 6. 前端：文件级审计要点（含行号定位）

### 6.1 `frontend/src/App.tsx`

- P0：Suspense fallback 文案异常（见 P0-02）  
  - 定位：`frontend/src/App.tsx:103`
- 建议：为路由增加 `errorElement` 或全局 ErrorBoundary，避免懒加载失败时白屏  
  - 定位：`frontend/src/App.tsx:11-96`（router 定义处）

### 6.2 `frontend/src/pages/WritingPage.tsx` 与 `frontend/src/pages/writing/*`

- P0：追踪文件依赖未跟踪 hooks（见 P0-01）  
  - 定位：`frontend/src/pages/WritingPage.tsx:20-27`、`frontend/src/pages/writing/useBatchGeneration.ts:7`
- 建议：WritingPage 的拆分方向是正确的，但需要补齐：  
  - hooks 纳入仓库 + 单测（至少对“错误码解析/竞态保护/abort 行为”）  
  - 请求竞态治理（见 P1-03）

### 6.3 `frontend/src/pages/writing/useBatchGeneration.ts`

- 问题：强制 `require_sequential: true`，忽略用户 UI 的“严格顺序”开关（如果 UI 有这个开关，会造成困惑）  
  - 定位：`frontend/src/pages/writing/useBatchGeneration.ts:96-105`（其中 `:102`）
- 建议：批量生成要么移除该开关（明确“批量必严格顺序”），要么尊重 `genForm.context.require_sequential`。

### 6.4 `frontend/src/hooks/useProjectData.ts`

- 问题：缺少请求竞态保护（见 P1-03）  
  - 定位：`frontend/src/hooks/useProjectData.ts:27-41`

### 6.5 `frontend/src/components/writing/AiGenerateDrawer.tsx`

- 问题：`useEffect` 依赖包含未使用变量 `generating`，会导致无谓的事件监听重绑  
  - 定位：`frontend/src/components/writing/AiGenerateDrawer.tsx:29-38`
- 问题：数字输入用 `Number(e.target.value)` 处理空值时语义不清（见 P2-03）  
  - 定位：`frontend/src/components/writing/AiGenerateDrawer.tsx:98-108`

### 6.6 `frontend/src/pages/PromptStudioPage.tsx`

- 问题：`reloadAll` 的 useCallback 依赖 `selectedPresetId`，而 `useEffect(() => reloadAll())` 依赖 reloadAll，会导致“选择 preset 时触发全量 reload”的额外网络与状态回填风险  
  - 定位：`frontend/src/pages/PromptStudioPage.tsx:183-216`
- 建议：把“全量 reload”与“选择态同步”解耦，避免无意触发全量刷新。

---

## 7. 推荐重构路线图（从 P0 到生产级）

### Phase 0：先把仓库变成“可交付状态”（只做最小必要修复）
- [ ] 完成 P0-01（追踪文件不再依赖未跟踪源码）
- [ ] 修复 P0-02（fallback 文案）
- [ ] 修复 P0-03（Gemini key 传递方式）
- [ ] 再跑一遍验收命令：  
  - 后端：`python -m compileall -q backend/app && PYTHONPATH=backend python -m unittest discover -s backend/tests -p "test_*.py" -v`  
  - 前端：`npm run lint && npm test && npx tsc -p tsconfig.app.json`

### Phase 1：可维护性“止血”（把大文件拆开，把竞态/幂等补齐）
- [ ] 完成 P1-02（LLM provider adapters 拆分）
- [ ] 完成 P1-03（前端请求竞态治理）
- [ ] 为“生成/流式/批量/导出”补齐最小回归测试矩阵（至少 MockTransport + vitest 单测）

### Phase 2：生产化能力建设（安全/可靠性/可观测）
- [ ] 引入真正的用户体系（AuthN/AuthZ），替换 `LOCAL_USER_ID`（`backend/app/api/deps.py:17-24`）
- [ ] 批量任务迁移到可靠队列（见 P1-04）
- [ ] 增加速率限制、审计、指标（latency/token/cost）、trace（request_id → generation_run_id）
- [ ] DB 迁移为 Postgres（或至少设计成可迁移）

---

## 附录 A：本次逐文件审计清单（追踪文件）

> 说明：A.1 清单来自 `git ls-files`（已逐个检查）；用于“范围透明化”和后续拆分任务的可追溯性。

### A.1 追踪文件列表（含行数）

后端（含 alembic/scripts/tests 与依赖清单）：
- backend/alembic.ini (38 lines)
- backend/alembic/env.py (103 lines)
- backend/alembic/versions/0f24b611cf21_init.py (176 lines)
- backend/alembic/versions/1c2a0e6b4c2d_multi_outlines_and_llm_profiles.py (192 lines)
- backend/alembic/versions/2c72bcfbae89_import_prompt_templates_into_prompt_.py (288 lines)
- backend/alembic/versions/2f6b9f4a0c1e_drop_llm_profiles_api_key.py (32 lines)
- backend/alembic/versions/3d1c1e8a9f0b_add_llm_profiles_api_key_ciphertext.py (31 lines)
- backend/alembic/versions/8b4c2f3a1d9e_add_batch_generation_tasks.py (71 lines)
- backend/alembic/versions/d8993729ae5a_add_prompt_presets_and_blocks.py (73 lines)
- backend/alembic/versions/dfb3c3ed0847_merge_heads.py (27 lines)
- backend/alembic/versions/f078e253d338_add_generation_runs_prompt_render_log_.py (28 lines)
- backend/app/__init__.py (1 lines)
- backend/app/api/__init__.py (1 lines)
- backend/app/api/deps.py (71 lines)
- backend/app/api/router.py (39 lines)
- backend/app/api/routes/__init__.py (1 lines)
- backend/app/api/routes/batch_generation.py (244 lines)
- backend/app/api/routes/chapter_analysis.py (213 lines)
- backend/app/api/routes/chapters.py (1076 lines)
- backend/app/api/routes/characters.py (71 lines)
- backend/app/api/routes/export.py (130 lines)
- backend/app/api/routes/generation_runs.py (116 lines)
- backend/app/api/routes/health.py (15 lines)
- backend/app/api/routes/llm_preset.py (125 lines)
- backend/app/api/routes/llm_profiles.py (166 lines)
- backend/app/api/routes/llm.py (81 lines)
- backend/app/api/routes/outline.py (513 lines)
- backend/app/api/routes/outlines.py (146 lines)
- backend/app/api/routes/projects.py (263 lines)
- backend/app/api/routes/prompts.py (444 lines)
- backend/app/api/routes/settings.py (58 lines)
- backend/app/core/__init__.py (1 lines)
- backend/app/core/config.py (110 lines)
- backend/app/core/errors.py (37 lines)
- backend/app/core/logging.py (75 lines)
- backend/app/core/request_id.py (19 lines)
- backend/app/core/secrets.py (190 lines)
- backend/app/db/__init__.py (1 lines)
- backend/app/db/base.py (6 lines)
- backend/app/db/migrations.py (81 lines)
- backend/app/db/session.py (47 lines)
- backend/app/db/utils.py (13 lines)
- backend/app/llm/__init__.py (1 lines)
- backend/app/llm/client.py (1251 lines)
- backend/app/llm/http_client.py (30 lines)
- backend/app/llm/messages.py (66 lines)
- backend/app/llm/utils.py (22 lines)
- backend/app/main.py (187 lines)
- backend/app/models/__init__.py (28 lines)
- backend/app/models/batch_generation_task.py (45 lines)
- backend/app/models/chapter.py (28 lines)
- backend/app/models/character.py (23 lines)
- backend/app/models/generation_run.py (30 lines)
- backend/app/models/llm_preset.py (27 lines)
- backend/app/models/llm_profile.py (25 lines)
- backend/app/models/outline.py (22 lines)
- backend/app/models/project_settings.py (19 lines)
- backend/app/models/project.py (31 lines)
- backend/app/models/prompt_block.py (33 lines)
- backend/app/models/prompt_preset.py (24 lines)
- backend/app/models/user.py (19 lines)
- backend/app/schemas/__init__.py (1 lines)
- backend/app/schemas/base.py (9 lines)
- backend/app/schemas/batch_generation.py (49 lines)
- backend/app/schemas/chapter_analysis.py (26 lines)
- backend/app/schemas/chapter_generate.py (26 lines)
- backend/app/schemas/chapter_plan.py (11 lines)
- backend/app/schemas/chapters.py (48 lines)
- backend/app/schemas/characters.py (29 lines)
- backend/app/schemas/common.py (24 lines)
- backend/app/schemas/generation_runs.py (23 lines)
- backend/app/schemas/llm_preset.py (38 lines)
- backend/app/schemas/llm_profiles.py (32 lines)
- backend/app/schemas/llm_test.py (18 lines)
- backend/app/schemas/llm.py (5 lines)
- backend/app/schemas/outline_generate.py (16 lines)
- backend/app/schemas/outline.py (35 lines)
- backend/app/schemas/projects.py (31 lines)
- backend/app/schemas/prompt_presets.py (146 lines)
- backend/app/schemas/settings.py (17 lines)
- backend/app/services/__init__.py (1 lines)
- backend/app/services/batch_generation_service.py (461 lines)
- backend/app/services/chapter_context_service.py (545 lines)
- backend/app/services/generation_pipeline.py (181 lines)
- backend/app/services/generation_service.py (258 lines)
- backend/app/services/length_control.py (17 lines)
- backend/app/services/llm_key_resolver.py (76 lines)
- backend/app/services/outline_store.py (27 lines)
- backend/app/services/output_contracts.py (105 lines)
- backend/app/services/output_parsers.py (399 lines)
- backend/app/services/prompt_budget.py (46 lines)
- backend/app/services/prompt_presets.py (1844 lines)
- backend/app/services/prompt_store.py (21 lines)
- backend/app/services/prompting.py (395 lines)
- backend/app/services/run_store.py (56 lines)
- backend/app/utils/__init__.py (2 lines)
- backend/app/utils/sse_response.py (71 lines)
- backend/requirements.lock.txt (27 lines)
- backend/requirements.txt (9 lines)
- backend/scripts/migrate_llm_profile_secrets.py (72 lines)
- backend/scripts/verify_anthropic_max_tokens_retry.py (79 lines)
- backend/tests/test_config_database_url.py (24 lines)
- backend/tests/test_generation_service_records_errors.py (46 lines)
- backend/tests/test_prompting_safe_template.py (75 lines)
- backend/tests/test_secrets_crypto.py (72 lines)

前端（含配置与测试）：
- frontend/.prettierrc.json (6 lines)
- frontend/eslint.config.js (23 lines)
- frontend/package-lock.json (6088 lines)
- frontend/package.json (45 lines)
- frontend/postcss.config.js (6 lines)
- frontend/README.md (73 lines)
- frontend/src/App.css (42 lines)
- frontend/src/App.tsx (110 lines)
- frontend/src/components/atelier/GhostwriterIndicator.tsx (23 lines)
- frontend/src/components/atelier/MarkdownEditor.tsx (114 lines)
- frontend/src/components/atelier/ProjectSwitcher.tsx (46 lines)
- frontend/src/components/atelier/ThemeToggle.tsx (45 lines)
- frontend/src/components/atelier/WizardNextBar.tsx (251 lines)
- frontend/src/components/layout/AppShell.tsx (380 lines)
- frontend/src/components/layout/ProjectProviderGuard.tsx (23 lines)
- frontend/src/components/prompts/LlmPresetPanel.tsx (311 lines)
- frontend/src/components/prompts/types.ts (16 lines)
- frontend/src/components/ui/confirm.ts (29 lines)
- frontend/src/components/ui/ConfirmProvider.tsx (84 lines)
- frontend/src/components/ui/Drawer.tsx (68 lines)
- frontend/src/components/ui/Modal.tsx (37 lines)
- frontend/src/components/ui/Overlay.tsx (35 lines)
- frontend/src/components/ui/toast.ts (22 lines)
- frontend/src/components/ui/ToastProvider.tsx (147 lines)
- frontend/src/components/writing/AiGenerateDrawer.tsx (323 lines)
- frontend/src/components/writing/BatchGenerationModal.tsx (149 lines)
- frontend/src/components/writing/ChapterAnalysisModal.tsx (252 lines)
- frontend/src/components/writing/ChapterListPanel.tsx (40 lines)
- frontend/src/components/writing/CreateChapterDialog.tsx (90 lines)
- frontend/src/components/writing/GenerationHistoryDrawer.tsx (148 lines)
- frontend/src/components/writing/types.ts (107 lines)
- frontend/src/components/writing/WritingToolbar.tsx (55 lines)
- frontend/src/contexts/projects.ts (17 lines)
- frontend/src/contexts/ProjectsContext.tsx (28 lines)
- frontend/src/hooks/useProjectData.ts (54 lines)
- frontend/src/hooks/useSaveHotkey.ts (15 lines)
- frontend/src/hooks/useUnsavedChangesGuard.tsx (34 lines)
- frontend/src/hooks/useWizardProgress.ts (74 lines)
- frontend/src/index.css (187 lines)
- frontend/src/lib/motion.ts (30 lines)
- frontend/src/main.tsx (10 lines)
- frontend/src/pages/CharactersPage.tsx (286 lines)
- frontend/src/pages/DashboardPage.tsx (294 lines)
- frontend/src/pages/ExportPage.tsx (174 lines)
- frontend/src/pages/NotFoundPage.tsx (12 lines)
- frontend/src/pages/OutlinePage.tsx (825 lines)
- frontend/src/pages/PreviewPage.tsx (246 lines)
- frontend/src/pages/ProjectWizardPage.tsx (358 lines)
- frontend/src/pages/PromptsPage.tsx (620 lines)
- frontend/src/pages/PromptStudioPage.tsx (1171 lines)
- frontend/src/pages/SettingsPage.tsx (218 lines)
- frontend/src/pages/writing/useBatchGeneration.ts (192 lines)
- frontend/src/pages/writing/useChapterAnalysis.ts (160 lines)
- frontend/src/pages/writing/useGenerationHistory.ts (54 lines)
- frontend/src/pages/writing/writingUtils.ts (36 lines)
- frontend/src/pages/WritingPage.tsx (553 lines)
- frontend/src/services/apiClient.ts (204 lines)
- frontend/src/services/chapterMarkerStreamParser.test.ts (79 lines)
- frontend/src/services/chapterMarkerStreamParser.ts (112 lines)
- frontend/src/services/currentUser.ts (8 lines)
- frontend/src/services/sseClient.ts (203 lines)
- frontend/src/services/storageKeys.ts (8 lines)
- frontend/src/services/theme.ts (35 lines)
- frontend/src/services/uiState.ts (9 lines)
- frontend/src/services/wizard.ts (357 lines)
- frontend/src/types.ts (157 lines)
- frontend/tailwind.config.js (34 lines)
- frontend/tsconfig.app.json (28 lines)
- frontend/tsconfig.json (4 lines)
- frontend/tsconfig.node.json (26 lines)
- frontend/vite.config.ts (15 lines)
- frontend/vitest.config.ts (7 lines)

### A.2 当前工作区“未跟踪但被引用”的源码（必须纳入仓库或回滚）

- frontend/src/pages/writing/useApplyGenerationRun.ts
- frontend/src/pages/writing/useChapterCrud.ts
- frontend/src/pages/writing/useChapterEditor.ts
- frontend/src/pages/writing/useChapterGeneration.ts
- frontend/src/pages/writing/useOutlineSwitcher.ts
- frontend/src/pages/writing/writingErrorUtils.ts
- frontend/src/pages/writing/writingErrorUtils.test.ts

---

## 附录 B：建议新增的“验收用例”（避免重构后功能悄悄坏掉）

后端（unittest / MockTransport）：
- [ ] OpenAI：400/422 时 downgrade steps 生效（stop/top_p/temperature/max_tokens 的丢弃/钳制）  
- [ ] Anthropic：从错误信息提取 max_tokens 上限并重试（仓库已有脚本可转为测试）  
- [ ] Gemini：header 传 key、streamGenerateContent（若引入）能解析输出  
- [ ] SSE：客户端断开时 generator 能正确 close，不写半条 generation_run

前端（vitest）：
- [ ] `extractMissingNumbers`（当前已写但未跟踪，纳入仓库后保留）  
- [ ] SSEPostClient：模拟 event-stream 分片/粘包/拆包场景，确保 progress/chunk/result/done 行为正确  
- [ ] Writing：切换章节竞态（先请求 A 再请求 B，A 晚返回不覆盖 B）
