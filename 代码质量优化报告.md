# 代码质量优化报告（ainovel MVP / Atelier）

> 审查日期：2026-01-02  
> 审查范围：`demo/` 仓库内“自有代码”（不含第三方依赖与构建产物）  
> 输出目标：以“Google 级别 Mentor”的标准，给出**可维护性/可靠性/安全性/一致性/可测试性**的行号级别问题清单与可执行重构任务拆分（含逐步校验）

---

## 0. 范围、证据与方法（保证可复现）

### 0.1 审查范围（包含/排除）

- **包含（本次逐行审查的自有代码）**
  - 后端：`backend/app/**/*.py`、`backend/alembic/**/*.py`、`backend/scripts/**/*.py`
  - 前端：`frontend/src/**/*.{ts,tsx,css}`
- **排除（不做逐行审查，但会做工程卫生检查）**
  - 第三方依赖：`frontend/node_modules/**`、`backend/.venv/**`
  - 构建产物：`frontend/dist/**`
  - Python 字节码：`**/__pycache__/**`、`**/*.pyc`

> 说明：对第三方依赖逐行 review 没有工程意义且不可控；本报告聚焦你团队实际维护的代码与可控配置。

### 0.2 仓库当前状态（非常关键）

当前工作区存在大量 **未提交修改** 与 **未跟踪新增文件**（这会影响“行号”的稳定性与后续 diff）。  
建议先把本次新增功能（例如批量生成/章节分析）整理为一个明确的提交，再进入重构周期。

- 代表性变更（节选，完整列表见 `git status`）：  
  - Modified：`backend/app/api/routes/chapters.py`、`backend/app/services/prompt_presets.py`、`frontend/src/pages/WritingPage.tsx` 等  
  - Untracked：`backend/app/services/batch_generation_service.py`、`backend/app/api/routes/batch_generation.py`、`backend/app/api/routes/chapter_analysis.py` 等  
  - Deleted（将由本报告文件替代）：`代码质量优化报告.md`

### 0.3 已执行的客观验证（命令/结果）

- 后端语法与导入：
  - `python -m compileall -q backend\app backend\alembic` ✅ 通过
  - `python -c "import fastapi, sqlalchemy, pydantic"` ✅ 通过（FastAPI 版本实际为 0.127.1）
- 前端工程：
  - `npm run lint` ✅ 通过
  - `npm test` ✅ 通过（1 个测试文件）
  - `npm run build` ✅ 通过，但有 **chunk > 500KB** 警告（见 §6.3）

---

## 1. 结论摘要（按严重级别）

### P0（阻断级：上线/对外暴露前必须修）

1. **服务端模板渲染存在“高风险 SSTI/RCE 面”**：当前 Prompt Studio 的模板渲染使用非沙箱 Jinja2，属于典型“用户可控模板 + 服务端执行”的安全红线。  
   - 位置：`backend/app/services/prompting.py:17,96,106`（详见 §2.1）
2. **API Key 加密策略在非 Windows 平台退化为 base64 明文**，并且存在“未知前缀当明文”的兼容路径，极易被误用到生产。  
   - 位置：`backend/app/core/secrets.py:33-60`（详见 §2.2）
3. **代码行尾风格严重不一致（同文件混合 CRLF/LF）**，导致行号/格式化/工具链行为不一致，已经直接影响 review、lint、diff、以及未来重构的可控性。  
   - 代表文件：`backend/app/llm/client.py`、`backend/app/api/routes/chapters.py`、`frontend/src/pages/WritingPage.tsx` 等（详见 §6.1）

### P1（高优先级：两周内必须做，否则会持续“越修越难”）

1. **前端关键页面巨型组件 + 明显格式漂移**：`WritingPage.tsx`（1773 行）与 `AiGenerateDrawer.tsx`（323 行）可读性已显著下降，维护风险极高。  
   - 位置：`frontend/src/pages/WritingPage.tsx:582-599,700-798`、`frontend/src/components/writing/AiGenerateDrawer.tsx:60-110`（详见 §3.1、§6.2）
2. **后端章节生成/流式生成/批量生成存在大量重复逻辑**，且部分逻辑已经出现轻微分叉（未来必然引入“修 A 坏 B”）。  
   - 位置：`backend/app/api/routes/chapters.py` vs `backend/app/services/batch_generation_service.py` vs `backend/app/api/routes/chapter_analysis.py`（详见 §2.4）
3. **异常与可观测性不足**：多处 `except Exception` 仅记录 `exception_type` 或直接吞掉细节，排障成本会指数上升。  
   - 位置：`backend/app/main.py:156-185`、`backend/app/api/routes/chapters.py:1447-1449`（详见 §2.3）

### P2（中优先级：可规划迭代）

- 前端构建产物过大，需要按路由/功能拆包（`vite` 已给出警告，详见 §6.3）
- 生成链路缺少“生成 run_id 的一致返回”，导致前端难以做“自动回放/应用/审计”体验闭环（详见 §2.5）

---

## 2. 后端（FastAPI / SQLAlchemy / LLM 适配）深度问题清单

### 2.1 P0｜安全：Prompt 模板渲染的 SSTI / RCE 风险（必须整改）

**问题描述**

`backend/app/services/prompting.py` 使用了非沙箱的 `jinja2.Environment` 来渲染模板：

- `backend/app/services/prompting.py:17`：`_JINJA = Environment(autoescape=False, ...)`
- `backend/app/services/prompting.py:96`：`ast = _JINJA.parse(...)`
- `backend/app/services/prompting.py:106`：`_JINJA.from_string(...).render(**values)`

这在“模板内容可由用户在 Prompt Studio 编辑/保存”的前提下，是典型的 **Server-Side Template Injection** 面。即使你现在只有 `LOCAL_USER_ID`（单用户本地），一旦未来：

- 这个服务被部署到服务器、  
- 或者加入真实用户体系、  
- 或者把 Prompt Studio 暴露给不完全可信的输入源，  

就会立刻具备高危攻击面（读取环境变量、文件、甚至执行任意代码——取决于 Jinja2 版本与可达对象链）。

**额外异常点（质量/可读性）**

`backend/app/services/prompting.py:102,112` 出现异常截断标记为 `"бн"`（西里尔字符），这属于明显的“混入垃圾字符/编码污染”。

**建议整改（拆成小任务）**

- **任务 P0-SEC-01：将模板引擎改为安全子集**
  1. 选型：
     - 方案 A（推荐）：改为“安全替换模板”——仅支持 `{{var}}` 与你自研的 macro（`{{date}}/{{pick::...}}` 等），**禁止任意表达式**。
     - 方案 B：使用 `jinja2.sandbox.SandboxedEnvironment` 并严格禁用危险属性/过滤器（仍需审计，且历史上有逃逸风险）。
  2. 迁移策略：
     - 统计当前 DB 中 PromptBlock.template 是否使用复杂 Jinja 语法（`{% ... %}` / `{{ a.b() }}` / `|` filter 等）。
     - 对不支持的语法给出清晰报错并提供迁移指南。
  3. 校验方式：
     - 新增“恶意模板用例”单元测试（至少覆盖 `__class__` / `__mro__` / `__subclasses__` 等典型链），确保渲染被阻断或输出被安全替换。
     - 手工：在 Prompt Studio 的“预览”接口渲染一次默认 preset，确认行为一致。

- **任务 P0-SEC-02：清理异常截断符**
  - 将 `"бн"` 统一改为标准省略号 `"…" / "..."`，并补充“为什么截断”的注释或常量。
  - 校验：运行 `python -m compileall -q backend\app`。

---

### 2.2 P0｜安全：API Key 加密/存储策略不可用于生产（需明确分层）

**问题描述**

`backend/app/core/secrets.py` 当前策略：

- Windows：使用 DPAPI（`encrypt_secret()` 在 `sys.platform == "win32"` 分支）  
  - 位置：`backend/app/core/secrets.py:33-41`
- 非 Windows：退化为 `plain:` + base64（**不是加密**）  
  - 位置：`backend/app/core/secrets.py:40`
- 解密：对未知前缀直接当明文（兼容路径）  
  - 位置：`backend/app/core/secrets.py:58-60`

**风险**

1. **非 Windows 环境等同明文存储**（base64 可逆）。  
2. **DPAPI 与“机器/用户上下文”绑定**：数据库迁移、容器化部署、服务账号变更时会导致 API Key 无法解密（你在 `resolve_api_key_for_profile()` 已捕获并提示重存，这是“坏体验”而非解决方案）。  
3. “未知前缀当明文”的兼容行为会让团队成员误以为“已经加密”，从而在生产中踩坑。

**建议整改（拆成小任务）**

- **任务 P0-SEC-03：引入明确的“加密密钥”与“环境分层”**
  1. 设计：
     - `APP_ENV=dev`：允许 DPAPI（仅限本地单机），但要在文档中明确“不可上生产”。
     - `APP_ENV=prod`：要求提供 `SECRET_ENCRYPTION_KEY`（例如 Fernet / AES-GCM），并拒绝 `plain:` 写入。
  2. 数据迁移：
     - 增加一次性迁移脚本：将历史 `plain:` ciphertext 重新加密为 `enc:` 前缀。
     - 对无法解密的旧数据给出可操作提示。
  3. 校验：
     - 单测：同一条密文在不同进程/机器/容器是否可解密（prod 方案必须可迁移）。
     - 手工：保存 API Key → 重启后端 → 再次调用生成。

- **任务 P0-SEC-04：删除“未知前缀当明文”或限制仅 dev**
  - 位置：`backend/app/core/secrets.py:58-60`
  - 校验：强制所有新写入都带前缀；老数据通过迁移处理。

---

### 2.3 P1｜可观测性：异常日志缺少关键信息（排障成本过高）

**问题描述**

全局异常处理只记录 `exception_type`，没有错误 message、没有 stacktrace（至少在 dev 环境）。例如：

- `backend/app/main.py:156-169`（SQLAlchemyError handler）
- `backend/app/main.py:172-185`（Exception handler）

以及流式生成：

- `backend/app/api/routes/chapters.py:1447-1449`：`except Exception:` 仅返回 SSE error，不记录异常细节

**影响**

- 线上偶发问题（DB 锁、第三方返回奇怪结构、编码异常）会变成“无法复盘”。
- 你现在做了 request_id，但没有“异常关联信息”，价值被浪费。

**建议整改（拆成小任务）**

- **任务 P1-OBS-01：在 dev 环境记录 stacktrace，在 prod 环境做安全裁剪**
  1. dev：`log_event(..., exception=str(exc), stack=traceback.format_exc())`（注意脱敏）。
  2. prod：记录 `exception_type` + `exception_hash`（hash(message)），避免泄露敏感信息。
  3. 校验：手工触发一个错误（例如传非法 base_url）能在日志看到可定位信息。

- **任务 P1-OBS-02：SSE 生成失败也要写 generation_run（含 error_json）**
  - 当前 `chapters.py` 对部分路径有补写（`backend/app/api/routes/chapters.py:1424-1444`），但 `except Exception` 分支仍丢失 run。
  - 校验：人为让 LLM 连接失败，检查 generation_runs 是否出现 error 记录。

---

### 2.4 P1｜重复逻辑：章节生成链路被复制了 3 份（未来一定出事故）

**重复/分叉点（代表性）**

1. 智能上下文构建：
   - `backend/app/api/routes/chapters.py:137-225`：`_build_smart_context()`
   - `backend/app/services/batch_generation_service.py:117-215`：`_build_smart_context()`
   - `backend/app/api/routes/chapter_analysis.py` 直接 `from ...chapters import _build_smart_context`（跨路由复用属于“架构味道”）

2. “plan_first → 再渲染 chapter_generate → 再 post_edit”流程：
   - `backend/app/api/routes/chapters.py:723-918`（非流式）
   - `backend/app/api/routes/chapters.py:1103-1451`（流式）
   - `backend/app/services/batch_generation_service.py:463-621`（批量）

3. 轻微死代码/冗余逻辑：
   - `backend/app/api/routes/chapters.py:200-201`：`range(1, chapter_number, stride)` 本就保证 `< chapter_number`，该 if 分支永远不成立。

**建议整改（拆成小任务）**

- **任务 P1-ARCH-01：抽出“章节上下文构建器”**
  - 新建服务：`chapter_context_service.py`
  - 输入：project_id、chapter_id、ChapterGenerateContext、mode、instruction、target_word_count…
  - 输出：统一的 `render_values`（含 project/story/user 三层命名空间）+ 校验信息
  - 校验：对比重构前后 `render_log`（`prompt_tokens_estimate`、blocks 数量、missing）保持一致。

- **任务 P1-ARCH-02：抽出“生成流水线（plan → generate → post_edit）”**
  - 让三条链路（JSON/Stream/Batch）共用同一 pipeline：
    - 仅差异：输出方式（一次性 JSON vs SSE chunk）与“是否允许 stream provider”
  - 校验：用同一输入在 JSON 与 Stream 下生成的最终 `content_md/summary` 应一致（至少在非流式 provider 下）。

- **任务 P1-ARCH-03：收敛 DB Session 打开/关闭策略**
  - 当前同一条请求里频繁 `SessionLocal()`（例如 `chapters.py:410`、`chapters.py:807`、`chapters.py:850`、`chapters.py:933`、`chapters.py:1217`、`chapters.py:1343`），阅读成本高且易漏 close。
  - 建议统一为：
    - “准备阶段”用短 session（读 DB、渲染 prompt）
    - “LLM 调用阶段”不持有 session
    - “落库阶段”用短 session（写 run / 写 task 状态）
  - 校验：并发压测（至少 5 并发）下 SQLite 不出现长时间锁（即使仍会偶发 locked，也要显著降低概率）。

---

### 2.5 P2｜接口一致性：生成类接口未统一返回 generation_run_id

**现状**

- `chapter_analyze` / `rewrite` 返回 `generation_run_id`（例如 `backend/app/api/routes/chapter_analysis.py` 在返回 data 中加入）
- `chapter_generate`（非流式）与 `outline_generate`（非流式）不返回 run_id（只能靠列表查询）

**建议**

- 所有“调用 LLM 并落库 generation_runs”的接口都应返回：
  - `generation_run_id`
  - `finish_reason`
  - `dropped_params`
  - `latency_ms`（若可获得）
  
**校验**

- 前端可在生成完成后立即提供“查看本次生成记录/一键应用/一键导出”的一致体验，而不必额外刷新列表。

---

### 2.6 P1｜可靠性：非 `AppError` 异常不会落 generation_run（审计链断裂）

**问题描述**

`backend/app/services/generation_service.py` 的 `call_llm_and_record()` 只捕获 `AppError` 并写入 `error_json`：

- `backend/app/services/generation_service.py:106`（函数入口）
- `backend/app/services/generation_service.py:187`（`except AppError as exc:`）

但当出现以下情况时：

- JSON 序列化/反序列化抛错（编码、上游返回畸形结构）
- 模板渲染抛错（当前为“吞异常继续”，但未来你大概率会收紧）
- 其它非 `AppError` 的程序异常（`KeyError/TypeError/ValueError/...`）

该次失败可能既：

- 没有把失败写入 `generation_runs`（或写入不完整）
- 又被全局异常处理抹平成 `INTERNAL_ERROR`

最终形成“审计链断裂”：**你无法追溯一次失败生成到底发生了什么**。

**建议整改（拆成小任务）**

- **任务 P1-REL-01：为 `call_llm_and_record` 增加兜底捕获并落库**
  1. 新增 `except Exception as exc:` 分支，写入 `generation_runs.error_json`：
     - `code="INTERNAL_ERROR"`
     - `details={"exception_type": type(exc).__name__}`（prod 不写 message；dev 可写裁剪后的 message）
  2. 校验：
     - 人为制造一个非 `AppError` 异常 → `generation_runs` 中应出现对应 `error_json`。

---

### 2.7 P2｜语义一致性：SSE 的 `word_count` 实际是字符数（命名误导）

**问题描述**

流式章节生成的进度事件携带 `word_count`，但填的是 `len(raw_output)`：

- `backend/app/api/routes/chapters.py:1269`：`word_count=len(raw_output)`

前端将其作为 `wordCount` 展示（`frontend/src/services/sseClient.ts` 的 onProgress 透传），这在：

- 英文（word != char）
- 或未来改为“按 token/字数精确控制”时  

会造成明显误导与进度条跳变。

**建议**

- 要么改名为 `char_count`
- 要么使用统一的“字数/词数估算器”（例如后端已有 `estimate_tokens()` / `estimate_max_tokens()` 的思路）

**校验**

- 中文与英文内容生成时，进度条与“计数”字段符合用户预期且可解释。

## 3. 前端（React + TS + Vite）深度问题清单

### 3.1 P1｜可维护性：WritingPage.tsx 巨型组件 + 代码风格明显漂移

**客观事实**

- `frontend/src/pages/WritingPage.tsx` 行数：1773（见附录 A）
- 该文件同时承担：
  - 数据加载（outline/preset/characters/chapters/runs/task polling）
  - 编辑器状态管理（baseline/form/dirty/切换保护）
  - 生成逻辑（流式+非流式+回退）
  - 批量生成任务管理（创建/取消/应用结果）
  - 分析与重写链路
  - UI 组装与交互

这会导致：任何小需求都会触发“跨 5 个概念域”的修改，review 成本与回归风险都极高。

**风格漂移（可读性已经受损）**

以下片段出现明显不符合 Prettier 的缩进/对齐，属于“不可长期维护”的信号：

- `frontend/src/pages/WritingPage.tsx:582-599`：payload 对象缩进错位（`plan_first/post_edit/context` 多行整体偏移）
- `frontend/src/pages/WritingPage.tsx:700-798`：错误分支缩进错位、花括号层级不直观

**潜在逻辑瑕疵（难以通过正常操作发现）**

- 取消流式生成时只回滚 `content_md/summary`，但不回滚 `status`（可能把原本 `done`/`planned` 的章节留在 `drafting` 状态）
  - 位置：`frontend/src/pages/WritingPage.tsx:668-670`

**建议整改（拆成小任务）**

- **任务 P1-FE-01：先做“无行为改变”的结构拆分**
  1. 抽出 hooks（不改 UI）：
     - `useChapterEditor()`：管理 baseline/form/dirty/save/applyRunId
     - `useChapterGeneration()`：管理 generate(stream+fallback)、cancel、progress
     - `useBatchGeneration()`：管理 task polling、create/cancel/apply
  2. 抽出 UI 组件：
     - `ChapterListPanel`
     - `EditorPanel`
     - `AnalysisPanel`
  3. 校验：
     - `npm run lint && npm test && npm run build` 全绿
     - 手工走一遍：创建章节→生成→取消→保存→预览→应用生成记录

- **任务 P1-FE-02：补齐格式化的“强制门禁”**
  - 让 `npm run format:check` 成为 CI 必跑项（或 pre-commit）。
  - 对现有文件统一 Prettier（这会改变行号，建议在完成 P0/P1 核心修复后再做一次“格式提交”）。

---

### 3.2 P1｜重复错误处理：missing_numbers 解析逻辑复制 3+ 次

同一段“从 ApiError.details 提取 missing_numbers”的逻辑在多处重复（未来字段名一变会炸多点）：

- 生成（流式异常处理）：`frontend/src/pages/WritingPage.tsx:708-735`
- 生成（非流式外层 catch）：`frontend/src/pages/WritingPage.tsx:765-792`
- 批量生成创建：`frontend/src/pages/WritingPage.tsx:975-1001`

**建议**

- 抽出工具函数：`extractMissingNumbers(err: unknown): number[]`
- 校验：模拟返回 `{ code: "CHAPTER_PREREQ_MISSING", details: { missing_numbers: [...] } }` 时只维护一处即可。

---

### 3.3 P2｜UI/动效：MarkdownEditor 的 LayoutGroup/layoutId 可能发生跨实例串扰

`frontend/src/components/atelier/MarkdownEditor.tsx`：

- `LayoutGroup id="atelier-markdown-editor-tabs"`：`frontend/src/components/atelier/MarkdownEditor.tsx:48`
- `layoutId="atelier-markdown-editor-tab"`：`frontend/src/components/atelier/MarkdownEditor.tsx:60,77`

如果页面上同时存在多个 MarkdownEditor（例如未来 PromptStudio/对比预览/分栏编辑），framer-motion 的共享布局 id 可能导致 tab 指示器“从另一个编辑器飞过来”的串扰。

**建议**

- 去掉 `LayoutGroup` 的固定 id，或为每个实例生成唯一 id（例如基于 `name`）。
- 校验：同时渲染 2 个 MarkdownEditor，切换 tab 不应互相影响。

---

### 3.4 P2｜ToastProvider：setTimeout 未集中管理，理论上可导致卸载后 setState

`frontend/src/components/ui/ToastProvider.tsx:31` 使用 `window.setTimeout(() => remove(id), ttl)`，未保存 timer id 做统一清理。

**建议**

- 在 Provider 内维护 `Map<string, number>` 存储 timer，remove 时 clear；组件卸载时清空所有 timer。
- 校验：快速切换路由卸载 Provider 时不出现 React state update warning。

---

### 3.5 P2｜API 客户端：错误码语义不准确 + 缺少超时/取消策略

**问题描述**

`frontend/src/services/apiClient.ts` 在“响应不是预期 JSON envelope”时抛出的错误码为 `DB_ERROR`：

- `frontend/src/services/apiClient.ts:75`：`code: "DB_ERROR", message: "响应格式错误"`
- `frontend/src/services/apiClient.ts:119`：`code: "DB_ERROR", message: "导出失败"`

这会导致前端错误面板与日志误判（并不是 DB 错）。

此外，`fetch()` 默认无超时：当后端卡死/网络半开连接时，UI 会长期挂起（尤其在生成/导出场景）。

**建议整改（拆成小任务）**

- **任务 P2-FE-API-01：统一错误码语义**
  - 把 `DB_ERROR` 替换为更准确的 `PROTOCOL_ERROR` / `BAD_RESPONSE` 等（并与后端 error.code 体系区分）。
  - 校验：后端返回 HTML/非 JSON 时，前端 toast 展示“响应格式错误（PROTOCOL_ERROR）”。

- **任务 P2-FE-API-02：为长请求引入 AbortController + 超时**
  - 对非 SSE 的 POST（生成/导出）提供可配置超时（例如 60-180s）。
  - 校验：人为把后端 endpoint sleep 过长 → 前端能在超时后给出可操作错误。

## 4. 批量生成（新功能）专项风险评估

### 4.1 架构风险：BackgroundTasks 不适合长任务（但 MVP 可接受）

`backend/app/api/routes/batch_generation.py` 使用 `BackgroundTasks.add_task(run_batch_generation_task, ...)`。

**风险**

- 进程重启/崩溃任务直接丢失（任务表仍是 running/queued）
- 并发与资源隔离不可控（同进程线程池争用）

**MVP 建议**

- 至少保证“任务恢复/重试/超时标记”：
  - 任务启动写入 `started_at`（或 updated_at + status）
  - worker 启动时扫描 `running` 超时任务 → 标记 failed/canceled
- 中长期：迁移到独立 worker（Celery/RQ/自建队列）

---

## 5. 代码风格与类型系统（质量门禁）

### 5.1 Python：大量 `except Exception` 与 `type: ignore` 降低类型价值

代表性位置：

- 广义异常吞掉：
  - `backend/app/services/generation_service.py:41,53`（JSON 解析失败直接回空，建议捕获 `json.JSONDecodeError`）
  - `backend/app/services/output_parsers.py:137`（`int(...)` 建议捕获 `ValueError/TypeError`）
  - `backend/app/api/routes/outline.py:51,84`（JSON loads 捕获应更精确）
- 类型逃逸：
  - `backend/app/api/routes/chapters.py:510,732,742,812,867,1112,1122,1222,1360` 等多处 `# type: ignore[arg-type]`

**建议**

- 把 `render_preset_for_task(..., values=...)` 的入参从 `dict[str, Any]` 改为 `Mapping[str, Any]`（避免 dict 的不变性导致 `dict[str, object]` 不能传入）。
- 校验：删除 `type: ignore[arg-type]` 后 `python -m compileall` 仍通过（并用 mypy/pyright 做进一步门禁）。

---

## 6. 工程卫生（会长期拖慢团队的“隐形成本”）

### 6.1 P0｜混合行尾（CRLF/LF）导致“行号不稳定/工具行为不一致”

扫描结果（仅自有代码 146 个文件）：

- 扫描文件数：146
- **混合行尾文件数：38**

代表性混合行尾文件（节选）：

- `backend/app/api/routes/chapters.py`（CRLF=881，lone LF=570）
- `backend/app/services/prompt_presets.py`（CRLF=918，lone LF=926）
- `backend/app/llm/client.py`（CRLF=719，lone LF=532）
- `frontend/src/pages/WritingPage.tsx`（CRLF=782，lone LF=991）
- `frontend/src/components/writing/AiGenerateDrawer.tsx`（CRLF=217，lone LF=106）

**影响**

- 不同工具对“行”的识别不同：你已经看到 PowerShell 与 Python 的行数不一致。
- Prettier/ESLint/编辑器格式化结果可能出现“每次都在改行尾”的噪音 diff。
- 本报告的“行号级定位”会在未来变得不可信。

**建议整改（拆成小任务）**

- **任务 P0-ENG-01：引入 `.gitattributes` 统一 eol**
  - 建议：全仓文本强制 `eol=lf`；仅对 `.bat/.ps1` 等必要文件保留 `crlf`。
- **任务 P0-ENG-02：一次性规范化行尾并单独提交**
  - 这是“纯格式提交”，必须与业务逻辑提交分开，避免 review 地狱。
- **校验**
  - `git diff` 中不再出现“整文件被改但逻辑不变”的噪音。

---

### 6.2 P1｜前端格式化未被强制执行

虽然你有 Prettier 配置（`frontend/.prettierrc.json`），但当前存在明显未格式化文件（见 §3.1、AiGenerateDrawer）。

**建议**

- `npm run format:check` 加入 CI/本地 pre-commit
- 对关键目录（`frontend/src/pages/**`、`frontend/src/components/**`）执行一次 Prettier 并提交

---

### 6.3 P2｜构建产物过大（性能/首屏）

`npm run build` 警告：Some chunks are larger than 500 kB after minification（实际主 chunk ~739KB）。

**建议**

- 路由级动态 import（React Router 支持懒加载）
- 把大页（`WritingPage/PromptStudioPage/OutlinePage`）按路由拆包
- 校验：`npm run build` 后主 chunk < 500KB（或明确设置阈值）

---

## 7. 建议的重构路线图（“小任务 + 校验”清单）

> 目标：开发只要按任务顺序逐个完成，并通过对应校验，即可把项目质量拉到长期可维护水平。

### 阶段 A（P0，1-3 天）：安全与工程稳定性

1. P0-SEC-01：模板引擎改为安全子集（§2.1）
2. P0-SEC-03/04：API Key 加密分层 + 禁止 plain（§2.2）
3. P0-ENG-01/02：统一行尾 + 单独提交（§6.1）

**阶段校验**

- 后端：`python -m compileall -q backend\app backend\alembic`
- 前端：`npm run lint && npm test && npm run build`
- 手工：Prompt Studio 预览 + 章节生成（含流式）

### 阶段 B（P1，3-7 天）：架构去重与可维护性

1. P1-ARCH-01：抽出 chapter context builder（§2.4）
2. P1-ARCH-02：抽出统一生成 pipeline（§2.4）
3. P1-FE-01：拆分 WritingPage（§3.1）
4. P1-FE-02：format check 门禁（§3.1、§6.2）

**阶段校验**

- 生成链路 JSON/Stream/Batch 三者输出一致性对比（至少对同一输入在非流式 provider 下）
- generation_runs 可追溯（每次失败也有记录）

### 阶段 C（P2，持续迭代）：性能与体验

1. P2：Vite 拆包（§6.3）
2. P2：Toast 计时器管理（§3.4）
3. P2：统一 generation_run_id 返回（§2.5）

---

## 附录 A：自有源文件清单（146 个文件 / 21877 行）

> 用于保证“没有漏文件”。注意：由于当前仓库存在混合行尾，建议先做 §6.1 的行尾规范化，再长期依赖此清单。

| Path | Lines |
|---|---:|
| backend\alembic\env.py | 69 |
| backend\alembic\versions\0f24b611cf21_init.py | 176 |
| backend\alembic\versions\1c2a0e6b4c2d_multi_outlines_and_llm_profiles.py | 192 |
| backend\alembic\versions\2c72bcfbae89_import_prompt_templates_into_prompt_.py | 288 |
| backend\alembic\versions\2f6b9f4a0c1e_drop_llm_profiles_api_key.py | 32 |
| backend\alembic\versions\3d1c1e8a9f0b_add_llm_profiles_api_key_ciphertext.py | 31 |
| backend\alembic\versions\8b4c2f3a1d9e_add_batch_generation_tasks.py | 71 |
| backend\alembic\versions\d8993729ae5a_add_prompt_presets_and_blocks.py | 73 |
| backend\alembic\versions\f078e253d338_add_generation_runs_prompt_render_log_.py | 28 |
| backend\app\__init__.py | 1 |
| backend\app\api\__init__.py | 1 |
| backend\app\api\deps.py | 71 |
| backend\app\api\router.py | 39 |
| backend\app\api\routes\__init__.py | 1 |
| backend\app\api\routes\batch_generation.py | 244 |
| backend\app\api\routes\chapter_analysis.py | 380 |
| backend\app\api\routes\chapters.py | 1451 |
| backend\app\api\routes\characters.py | 71 |
| backend\app\api\routes\export.py | 130 |
| backend\app\api\routes\generation_runs.py | 116 |
| backend\app\api\routes\health.py | 15 |
| backend\app\api\routes\llm.py | 81 |
| backend\app\api\routes\llm_preset.py | 125 |
| backend\app\api\routes\llm_profiles.py | 160 |
| backend\app\api\routes\outline.py | 505 |
| backend\app\api\routes\outlines.py | 146 |
| backend\app\api\routes\projects.py | 263 |
| backend\app\api\routes\prompts.py | 444 |
| backend\app\api\routes\settings.py | 58 |
| backend\app\core\__init__.py | 1 |
| backend\app\core\config.py | 57 |
| backend\app\core\errors.py | 37 |
| backend\app\core\logging.py | 32 |
| backend\app\core\request_id.py | 19 |
| backend\app\core\secrets.py | 155 |
| backend\app\db\__init__.py | 1 |
| backend\app\db\base.py | 6 |
| backend\app\db\migrations.py | 81 |
| backend\app\db\session.py | 47 |
| backend\app\db\utils.py | 13 |
| backend\app\llm\__init__.py | 1 |
| backend\app\llm\client.py | 1251 |
| backend\app\llm\http_client.py | 30 |
| backend\app\llm\messages.py | 66 |
| backend\app\llm\utils.py | 22 |
| backend\app\main.py | 187 |
| backend\app\models\__init__.py | 28 |
| backend\app\models\batch_generation_task.py | 45 |
| backend\app\models\chapter.py | 28 |
| backend\app\models\character.py | 23 |
| backend\app\models\generation_run.py | 30 |
| backend\app\models\llm_preset.py | 27 |
| backend\app\models\llm_profile.py | 25 |
| backend\app\models\outline.py | 22 |
| backend\app\models\project.py | 31 |
| backend\app\models\project_settings.py | 19 |
| backend\app\models\prompt_block.py | 33 |
| backend\app\models\prompt_preset.py | 24 |
| backend\app\models\user.py | 19 |
| backend\app\schemas\__init__.py | 1 |
| backend\app\schemas\base.py | 9 |
| backend\app\schemas\batch_generation.py | 49 |
| backend\app\schemas\chapter_analysis.py | 26 |
| backend\app\schemas\chapter_generate.py | 26 |
| backend\app\schemas\chapter_plan.py | 11 |
| backend\app\schemas\chapters.py | 48 |
| backend\app\schemas\characters.py | 29 |
| backend\app\schemas\common.py | 24 |
| backend\app\schemas\generation_runs.py | 23 |
| backend\app\schemas\llm.py | 5 |
| backend\app\schemas\llm_preset.py | 38 |
| backend\app\schemas\llm_profiles.py | 32 |
| backend\app\schemas\llm_test.py | 18 |
| backend\app\schemas\outline.py | 35 |
| backend\app\schemas\outline_generate.py | 16 |
| backend\app\schemas\projects.py | 31 |
| backend\app\schemas\prompt_presets.py | 146 |
| backend\app\schemas\settings.py | 17 |
| backend\app\services\__init__.py | 1 |
| backend\app\services\batch_generation_service.py | 675 |
| backend\app\services\generation_service.py | 218 |
| backend\app\services\length_control.py | 17 |
| backend\app\services\llm_key_resolver.py | 72 |
| backend\app\services\outline_store.py | 27 |
| backend\app\services\output_contracts.py | 105 |
| backend\app\services\output_parsers.py | 399 |
| backend\app\services\prompt_budget.py | 46 |
| backend\app\services\prompt_presets.py | 1844 |
| backend\app\services\prompt_store.py | 21 |
| backend\app\services\prompting.py | 119 |
| backend\app\services\run_store.py | 56 |
| backend\app\utils\__init__.py | 2 |
| backend\app\utils\sse_response.py | 71 |
| backend\scripts\verify_anthropic_max_tokens_retry.py | 79 |
| frontend\src\App.css | 42 |
| frontend\src\App.tsx | 57 |
| frontend\src\components\atelier\GhostwriterIndicator.tsx | 23 |
| frontend\src\components\atelier\MarkdownEditor.tsx | 112 |
| frontend\src\components\atelier\ProjectSwitcher.tsx | 46 |
| frontend\src\components\atelier\ThemeToggle.tsx | 45 |
| frontend\src\components\atelier\WizardNextBar.tsx | 242 |
| frontend\src\components\layout\AppShell.tsx | 380 |
| frontend\src\components\layout\ProjectProviderGuard.tsx | 23 |
| frontend\src\components\prompts\LlmPresetPanel.tsx | 311 |
| frontend\src\components\prompts\types.ts | 16 |
| frontend\src\components\ui\confirm.ts | 29 |
| frontend\src\components\ui\ConfirmProvider.tsx | 84 |
| frontend\src\components\ui\Drawer.tsx | 68 |
| frontend\src\components\ui\Modal.tsx | 37 |
| frontend\src\components\ui\Overlay.tsx | 35 |
| frontend\src\components\ui\toast.ts | 14 |
| frontend\src\components\ui\ToastProvider.tsx | 122 |
| frontend\src\components\writing\AiGenerateDrawer.tsx | 323 |
| frontend\src\components\writing\CreateChapterDialog.tsx | 90 |
| frontend\src\components\writing\GenerationHistoryDrawer.tsx | 148 |
| frontend\src\components\writing\types.ts | 107 |
| frontend\src\contexts\projects.ts | 17 |
| frontend\src\contexts\ProjectsContext.tsx | 28 |
| frontend\src\hooks\useProjectData.ts | 54 |
| frontend\src\hooks\useSaveHotkey.ts | 15 |
| frontend\src\hooks\useUnsavedChangesGuard.tsx | 34 |
| frontend\src\hooks\useWizardProgress.ts | 74 |
| frontend\src\index.css | 187 |
| frontend\src\lib\motion.ts | 30 |
| frontend\src\main.tsx | 10 |
| frontend\src\pages\CharactersPage.tsx | 286 |
| frontend\src\pages\DashboardPage.tsx | 294 |
| frontend\src\pages\ExportPage.tsx | 174 |
| frontend\src\pages\NotFoundPage.tsx | 12 |
| frontend\src\pages\OutlinePage.tsx | 825 |
| frontend\src\pages\PreviewPage.tsx | 249 |
| frontend\src\pages\ProjectWizardPage.tsx | 358 |
| frontend\src\pages\PromptsPage.tsx | 620 |
| frontend\src\pages\PromptStudioPage.tsx | 1175 |
| frontend\src\pages\SettingsPage.tsx | 218 |
| frontend\src\pages\WritingPage.tsx | 1773 |
| frontend\src\services\apiClient.ts | 165 |
| frontend\src\services\chapterMarkerStreamParser.test.ts | 79 |
| frontend\src\services\chapterMarkerStreamParser.ts | 112 |
| frontend\src\services\currentUser.ts | 8 |
| frontend\src\services\sseClient.ts | 202 |
| frontend\src\services\storageKeys.ts | 8 |
| frontend\src\services\theme.ts | 35 |
| frontend\src\services\uiState.ts | 9 |
| frontend\src\services\wizard.ts | 357 |
| frontend\src\types.ts | 157 |

---

## 附录 B：混合行尾（CRLF/LF）文件清单（38 个）

> 这类文件会导致不同工具（PowerShell / Python / Git / 编辑器）对“行号”的理解不一致。建议优先完成 §6.1 的行尾统一。

| Path | CRLF 行数 | lone LF 行数 |
|---|---:|---:|
| backend\app\api\router.py | 23 | 16 |
| backend\app\api\routes\chapters.py | 881 | 570 |
| backend\app\api\routes\generation_runs.py | 80 | 36 |
| backend\app\api\routes\llm.py | 19 | 62 |
| backend\app\api\routes\llm_preset.py | 104 | 21 |
| backend\app\api\routes\llm_profiles.py | 124 | 36 |
| backend\app\api\routes\outline.py | 450 | 55 |
| backend\app\api\routes\projects.py | 75 | 188 |
| backend\app\api\routes\prompts.py | 378 | 66 |
| backend\app\db\migrations.py | 52 | 29 |
| backend\app\llm\client.py | 719 | 532 |
| backend\app\llm\utils.py | 7 | 15 |
| backend\app\main.py | 131 | 56 |
| backend\app\models\__init__.py | 14 | 14 |
| backend\app\schemas\chapter_generate.py | 11 | 15 |
| backend\app\schemas\llm_test.py | 9 | 9 |
| backend\app\services\generation_service.py | 172 | 46 |
| backend\app\services\output_contracts.py | 42 | 63 |
| backend\app\services\output_parsers.py | 241 | 158 |
| backend\app\services\prompt_presets.py | 918 | 926 |
| backend\app\services\prompting.py | 112 | 7 |
| backend\app\services\run_store.py | 24 | 32 |
| frontend\src\components\atelier\MarkdownEditor.tsx | 35 | 77 |
| frontend\src\components\ui\Overlay.tsx | 24 | 11 |
| frontend\src\components\ui\toast.ts | 10 | 4 |
| frontend\src\components\ui\ToastProvider.tsx | 76 | 46 |
| frontend\src\components\writing\AiGenerateDrawer.tsx | 217 | 106 |
| frontend\src\components\writing\types.ts | 16 | 91 |
| frontend\src\hooks\useWizardProgress.ts | 57 | 17 |
| frontend\src\pages\DashboardPage.tsx | 242 | 52 |
| frontend\src\pages\PreviewPage.tsx | 112 | 137 |
| frontend\src\pages\PromptsPage.tsx | 595 | 25 |
| frontend\src\pages\PromptStudioPage.tsx | 1079 | 96 |
| frontend\src\pages\WritingPage.tsx | 782 | 991 |
| frontend\src\services\apiClient.ts | 151 | 14 |
| frontend\src\services\sseClient.ts | 157 | 45 |
| frontend\src\services\wizard.ts | 228 | 129 |
| frontend\src\types.ts | 132 | 25 |
